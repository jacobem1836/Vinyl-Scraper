---
phase: 15-notification-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/database.py
  - app/models.py
  - app/config.py
autonomous: true
requirements: [NOTIF-01, NOTIF-02, NOTIF-03]

must_haves:
  truths:
    - "Six new columns exist in DB (2 on listings, 4 on wishlist_items) after app startup"
    - "ORM models expose new columns matching their migration types and nullability"
    - "New global settings are readable via settings.notify_drop_pct_default, notify_drop_usd_default, notify_cooldown_hours"
  artifacts:
    - path: app/database.py
      provides: "ALTER TABLE migrations for 6 new columns in run_migrations()"
      contains: "prev_price, prev_is_in_stock, last_notified_at, notify_drop_mode, notify_drop_pct, notify_drop_usd"
    - path: app/models.py
      provides: "ORM column declarations on Listing and WishlistItem"
      contains: "prev_price, prev_is_in_stock, last_notified_at, notify_drop_mode, notify_drop_pct, notify_drop_usd"
    - path: app/config.py
      provides: "New settings fields"
      contains: "notify_drop_pct_default, notify_drop_usd_default, notify_cooldown_hours"
  key_links:
    - from: app/database.py
      to: "listings and wishlist_items tables"
      via: "ALTER TABLE ADD COLUMN in try/except blocks (existing pattern)"
      pattern: "ALTER TABLE .* ADD COLUMN (prev_price|prev_is_in_stock|last_notified_at|notify_drop_mode|notify_drop_pct|notify_drop_usd)"
    - from: app/models.py
      to: "SQLAlchemy declarative base"
      via: "Column() declarations"
      pattern: "Column\\((Float|Boolean|DateTime|String)"
---

<objective>
Add the schema foundation for Phase 15: six new columns across `listings` and `wishlist_items`, matching SQLAlchemy ORM column declarations, and three new global settings in `config.py`.

Purpose: Every downstream task (scanner prev_ capture, notifier detection, scheduler cooldown) depends on these columns and settings existing. This plan is the foundation wave — no business logic, just schema + config.

Output: Updated database migrations, model, and config files with all new columns/settings declared.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-notification-expansion/15-CONTEXT.md
@.planning/phases/15-notification-expansion/15-RESEARCH.md

<interfaces>
Existing patterns to follow (from codebase):

app/database.py run_migrations() — try/except ALTER TABLE ADD COLUMN blocks, `pass` on exception (column already exists).

app/models.py Column pattern:
```python
# On Listing
price = Column(Float, nullable=True)
is_in_stock = Column(Boolean, nullable=False, default=True)

# On WishlistItem
notify_email = Column(Boolean, nullable=False, default=False)
notify_below_pct = Column(Float, nullable=False, default=20.0)
```

app/config.py — Settings(BaseSettings) class with typed fields and defaults.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add migrations + ORM columns for Listing prev_ tracking + WishlistItem notification fields</name>
  <files>app/database.py, app/models.py</files>
  <read_first>
    - app/database.py (see existing run_migrations() ALTER TABLE blocks — the pattern to replicate)
    - app/models.py (see existing Listing and WishlistItem column declarations)
    - .planning/phases/15-notification-expansion/15-RESEARCH.md §"Migration SQL for Six New Columns" (exact SQL to use)
    - .planning/phases/15-notification-expansion/15-CONTEXT.md D-02, D-03, D-05, D-06, D-08 (column definitions)
  </read_first>
  <action>
    Per D-02, D-03, D-05, D-06, D-08: add six columns.

    In app/database.py run_migrations(), append six new try/except ALTER TABLE blocks (copy exact SQL from RESEARCH.md §"Migration SQL for Six New Columns"):
    1. `ALTER TABLE listings ADD COLUMN prev_price FLOAT`
    2. `ALTER TABLE listings ADD COLUMN prev_is_in_stock INTEGER` (nullable, no DEFAULT — per pitfall 2 in RESEARCH.md)
    3. `ALTER TABLE wishlist_items ADD COLUMN last_notified_at DATETIME`
    4. `ALTER TABLE wishlist_items ADD COLUMN notify_drop_mode VARCHAR NOT NULL DEFAULT 'pct'`
    5. `ALTER TABLE wishlist_items ADD COLUMN notify_drop_pct FLOAT`
    6. `ALTER TABLE wishlist_items ADD COLUMN notify_drop_usd FLOAT`

    Each in its own try/except Exception: pass block with conn.commit() on success. Follow the exact style of existing migrations in that file.

    In app/models.py:
    - On `Listing`: add `prev_price = Column(Float, nullable=True)` and `prev_is_in_stock = Column(Boolean, nullable=True)` (nullable True — None means "not yet observed").
    - On `WishlistItem`: add `last_notified_at = Column(DateTime, nullable=True)`, `notify_drop_mode = Column(String, nullable=False, default="pct")`, `notify_drop_pct = Column(Float, nullable=True)`, `notify_drop_usd = Column(Float, nullable=True)`.

    Ensure DateTime is imported from sqlalchemy in models.py if not already.

    Do NOT add any other logic. Do NOT modify existing columns.
  </action>
  <verify>
    <automated>python -c "from app.database import engine, run_migrations; run_migrations(engine); from app.models import Listing, WishlistItem; assert hasattr(Listing, 'prev_price') and hasattr(Listing, 'prev_is_in_stock'); assert hasattr(WishlistItem, 'last_notified_at') and hasattr(WishlistItem, 'notify_drop_mode') and hasattr(WishlistItem, 'notify_drop_pct') and hasattr(WishlistItem, 'notify_drop_usd'); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/database.py` contains 6 new try/except ALTER TABLE blocks matching RESEARCH.md SQL exactly
    - `app/models.py` Listing class has `prev_price` (Float, nullable) and `prev_is_in_stock` (Boolean, nullable) columns
    - `app/models.py` WishlistItem class has `last_notified_at` (DateTime, nullable), `notify_drop_mode` (String, default "pct"), `notify_drop_pct` (Float, nullable), `notify_drop_usd` (Float, nullable)
    - Running `run_migrations()` on a fresh DB succeeds; running twice does not raise
    - The automated verify command prints "OK"
  </acceptance_criteria>
  <done>Six columns migrated and declared on ORM; idempotent on re-run.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add global notification settings to config.py</name>
  <files>app/config.py</files>
  <read_first>
    - app/config.py (existing Settings class and field patterns)
    - .planning/phases/15-notification-expansion/15-CONTEXT.md D-04, D-09
    - .planning/phases/15-notification-expansion/15-RESEARCH.md §"Pattern 3: Pydantic Settings Addition" (default values)
  </read_first>
  <action>
    Per D-04 and D-09, add three new fields to the Settings class in app/config.py:

    ```python
    notify_drop_pct_default: float = 20.0
    notify_drop_usd_default: float = 5.0
    notify_cooldown_hours: int = 24
    ```

    Place them grouped logically near the existing `notify_*` / notification-related settings (or, if no such group exists, near other numeric threshold settings like `shipping_estimate_usd`). Use the existing field declaration style in config.py (type annotation + default).

    Do NOT modify existing fields. Do NOT add env-var prefixes or validators beyond what the existing settings use.
  </action>
  <verify>
    <automated>python -c "from app.config import settings; assert settings.notify_drop_pct_default == 20.0; assert settings.notify_drop_usd_default == 5.0; assert settings.notify_cooldown_hours == 24; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/config.py` Settings class has three new fields: `notify_drop_pct_default: float = 20.0`, `notify_drop_usd_default: float = 5.0`, `notify_cooldown_hours: int = 24`
    - Settings instance loads without error
    - Environment variables `NOTIFY_DROP_PCT_DEFAULT`, `NOTIFY_DROP_USD_DEFAULT`, `NOTIFY_COOLDOWN_HOURS` can override (via pydantic-settings default behaviour)
    - The automated verify command prints "OK"
  </acceptance_criteria>
  <done>Three new settings exist with documented defaults and env override support.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → config | Settings read from .env / environment; already trusted (no new user-input surface) |
| scheduler → DB | Internal; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | I (Info Disclosure) | app/config.py new settings | accept | Defaults are non-sensitive numeric thresholds; no PII or secrets added |
| T-15-02 | T (Tampering) | ALTER TABLE migrations | accept | Migrations are server-side, run on startup only; no user-input path |
</threat_model>

<verification>
- `run_migrations()` idempotent (runs twice without error)
- All six columns queryable via ORM
- All three settings readable from `settings` instance
</verification>

<success_criteria>
Foundation schema + config in place so Plans 02, 03, 04 can reference prev_price, prev_is_in_stock, last_notified_at, notify_drop_mode, notify_drop_pct, notify_drop_usd columns, and notify_drop_pct_default, notify_drop_usd_default, notify_cooldown_hours settings.
</success_criteria>

<output>
After completion, create `.planning/phases/15-notification-expansion/15-01-SUMMARY.md`
</output>

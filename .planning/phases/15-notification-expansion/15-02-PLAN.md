---
phase: 15-notification-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_notifier.py
autonomous: true
requirements: [NOTIF-01, NOTIF-02, NOTIF-03, NOTIF-04]

must_haves:
  truths:
    - "Test scaffold exists with failing tests covering all NOTIF-* behaviours listed in RESEARCH.md §Phase Requirements → Test Map"
    - "Running pytest returns red (tests fail because helpers don't exist yet) — proving tests actually exercise target code paths"
    - "Shared fixtures produce mock WishlistItem and Listing instances with prev_ columns set to specified values"
  artifacts:
    - path: tests/test_notifier.py
      provides: "Unit tests for _price_dropped, _back_in_stock, _within_cooldown, send_digest_email aggregation"
      min_lines: 80
    - path: tests/conftest.py
      provides: "Shared fixtures (mock WishlistItem, mock Listing, monkeypatched settings)"
      min_lines: 20
    - path: tests/__init__.py
      provides: "Empty init so pytest discovers the package"
  key_links:
    - from: tests/test_notifier.py
      to: app.services.notifier
      via: "import (will be unresolved until Plan 03 adds helpers — this is expected RED state)"
      pattern: "from app.services.notifier import"
---

<objective>
Create failing unit tests for Phase 15 notification behaviours BEFORE implementation, so Plans 03 and 04 can use `pytest tests/test_notifier.py -x` as their GREEN signal.

Purpose: Enforces RED→GREEN discipline. Plan 03 (which implements the helpers) has a concrete contract — the tests in this plan — rather than planning-time guesses about signatures.

Output: tests/ package with test_notifier.py covering the 9 cases listed in RESEARCH.md §"Phase Requirements → Test Map", plus conftest.py fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-notification-expansion/15-CONTEXT.md
@.planning/phases/15-notification-expansion/15-RESEARCH.md

<interfaces>
Helper signatures that tests will target (to be implemented in Plan 03 of this phase):

```python
# In app/services/notifier.py (added by Plan 03)
def _price_dropped(listing: Listing, item: WishlistItem) -> bool: ...
def _back_in_stock(listing: Listing) -> bool: ...
def _within_cooldown(last_notified_at: datetime | None) -> bool: ...
async def send_digest_email(digest_items: list[tuple[WishlistItem, dict]]) -> bool: ...
```

Event dict shape (from RESEARCH.md §Pattern 6):
```python
{
    "deal_alerts": [Listing, ...],
    "price_drops": [(Listing, float, float), ...],  # (listing, prev_price, new_price)
    "back_in_stock": [Listing, ...],
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/ package scaffold + shared fixtures</name>
  <files>tests/__init__.py, tests/conftest.py</files>
  <read_first>
    - app/models.py (Listing and WishlistItem current columns — fixtures must instantiate them)
    - app/config.py (Settings class — for monkeypatching notify_cooldown_hours in tests)
  </read_first>
  <behavior>
    - Fixture `mock_item` returns a WishlistItem-like object with notify_email=True, notify_below_pct=20.0, notify_drop_mode="pct", notify_drop_pct=None, notify_drop_usd=None, last_notified_at=None, listings=[].
    - Fixture `mock_listing` returns a Listing-like object factory accepting price, prev_price, is_in_stock, prev_is_in_stock as kwargs.
    - Fixture `settings_override` monkeypatches settings.notify_cooldown_hours, settings.notify_drop_pct_default, settings.notify_drop_usd_default.
  </behavior>
  <action>
    Create empty `tests/__init__.py`.

    Create `tests/conftest.py` with three fixtures:
    - `mock_item`: returns a plain object (SimpleNamespace or Mock) with the WishlistItem attributes listed in the behavior section. Do NOT use the real SQLAlchemy model — avoid DB coupling.
    - `mock_listing`: a factory fixture (returns a function). The function accepts kwargs and returns a SimpleNamespace with `price`, `prev_price`, `is_in_stock`, `prev_is_in_stock`, `is_active=True`, `ships_from=None`, and any other fields the notifier helpers read. Default all prev_ fields to None.
    - `settings_override`: a fixture that monkeypatches `app.config.settings` attributes for the test, restoring after. Accepts kwargs via indirect parametrization OR returns a helper function.

    Use `from types import SimpleNamespace`. Import Listing/WishlistItem types only for type hints if needed. Keep conftest under 60 lines.
  </action>
  <verify>
    <automated>cd /Users/jacobmarriott/Documents/Personal/Vinyl-Scraper && python -m pytest tests/ --collect-only -q 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/__init__.py` exists (empty)
    - `tests/conftest.py` defines `mock_item`, `mock_listing`, `settings_override` fixtures
    - `pytest --collect-only` runs without import errors (fixtures themselves compile)
    - conftest.py has no dependency on a live database
  </acceptance_criteria>
  <done>Scaffold in place; fixtures importable by test_notifier.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing unit tests for NOTIF-01 through NOTIF-04</name>
  <files>tests/test_notifier.py</files>
  <read_first>
    - tests/conftest.py (fixtures from Task 1)
    - .planning/phases/15-notification-expansion/15-RESEARCH.md §"Phase Requirements → Test Map" (the 9 test cases to implement)
    - .planning/phases/15-notification-expansion/15-RESEARCH.md §"Code Examples" (the detection logic that tests must encode)
    - .planning/phases/15-notification-expansion/15-CONTEXT.md D-01 through D-15
  </read_first>
  <behavior>
    Exactly these 9 tests, named per RESEARCH.md:
    - test_back_in_stock: prev_is_in_stock=False, is_in_stock=True → True
    - test_back_in_stock_first_scan: prev_is_in_stock=None, is_in_stock=True → False
    - test_price_drop_pct: prev_price=100, price=70, mode=pct, threshold 20 → True (30% >= 20)
    - test_price_drop_usd: prev_price=100, price=93, mode=usd, threshold 5 → True ($7 >= $5)
    - test_price_drop_no_prev: prev_price=None → False
    - test_cooldown_suppresses: last_notified_at = now - 1h, cooldown=24h → True (within)
    - test_cooldown_expired: last_notified_at = now - 48h, cooldown=24h → False (outside)
    - test_no_digest_on_zero_events: send_digest_email called with [] or items all having empty events returns False/None and sends no email (mock _send_smtp)
    - test_digest_aggregates: send_digest_email called with 2 items, each with events → exactly one _send_smtp call, subject contains total event count
  </behavior>
  <action>
    Create `tests/test_notifier.py`. Per D-01 through D-15 and RESEARCH.md:

    Structure (AAA pattern, pytest):
    - Imports: `pytest`, `from datetime import datetime, timedelta`, `from unittest.mock import AsyncMock, patch`.
    - Import the helpers under test: `from app.services.notifier import _price_dropped, _back_in_stock, _within_cooldown, send_digest_email`. These DO NOT EXIST YET — tests will fail on import/collection. This is expected (RED state). Use `pytest.importorskip` or just let collection fail; do NOT stub the imports.

    Write the 9 tests listed in the behavior section. Use `mock_item`, `mock_listing`, `settings_override` fixtures. For the two digest tests, patch `app.services.notifier._send_smtp` with `AsyncMock` (or patch `asyncio.to_thread` if _send_smtp is called via to_thread) and assert call count.

    Each test is short (5-15 lines), AAA-structured, with a descriptive name.

    Do NOT implement the helpers. Do NOT add imports from future code that doesn't exist beyond the four target names.
  </action>
  <verify>
    <automated>cd /Users/jacobmarriott/Documents/Personal/Vinyl-Scraper && python -m pytest tests/test_notifier.py -x 2>&1 | tail -20 ; echo "EXPECTED: tests fail with ImportError or AttributeError on _price_dropped/_back_in_stock/_within_cooldown/send_digest_email — this is the RED state for Plan 03 to turn GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_notifier.py contains exactly the 9 test function names listed in the behavior section
    - Collection attempts `from app.services.notifier import _price_dropped, _back_in_stock, _within_cooldown, send_digest_email`
    - Running `pytest tests/test_notifier.py` produces a failure (RED) — ImportError acceptable, AttributeError acceptable. Failure is the expected outcome of this plan.
    - Tests use fixtures from conftest.py (no duplicated setup)
    - No test body contains a literal implementation of the helper under test (tests check behaviour, don't re-implement logic)
  </acceptance_criteria>
  <done>9 failing tests exist, targeting the exact signatures Plan 03 will implement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → code under test | Internal; tests do not exercise external I/O (SMTP mocked) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | I (Info Disclosure) | tests/ directory | accept | No secrets in fixtures; mock data only |
</threat_model>

<verification>
- `pytest --collect-only tests/` succeeds for conftest fixtures
- `pytest tests/test_notifier.py` fails (RED) — this plan intentionally ships failing tests
</verification>

<success_criteria>
Plan 03 has concrete test targets to turn GREEN. Plan 04 inherits a trusted test suite for regression guarding.
</success_criteria>

<output>
After completion, create `.planning/phases/15-notification-expansion/15-02-SUMMARY.md`
</output>

---
phase: 25-ebay-credentials
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - app/main.py
autonomous: false
requirements:
  - EBAY-01
  - EBAY-02
must_haves:
  truths:
    - ".env.example documents EBAY_APP_ID, EBAY_CERT_ID, and EBAY_DEV_ID with descriptions"
    - "Starting the app without eBay credentials prints a visible [startup] warning but the app still starts"
    - "A scan against an active wishlist item returns eBay AU listings alongside Discogs and Shopify results"
  artifacts:
    - path: ".env.example"
      provides: "Documented EBAY_APP_ID, EBAY_CERT_ID, EBAY_DEV_ID entries"
      contains: "EBAY_APP_ID"
    - path: "app/main.py"
      provides: "Startup warning when eBay credentials are absent"
      contains: "ebay"
  key_links:
    - from: "app/main.py startup()"
      to: "settings.ebay_app_id / settings.ebay_cert_id"
      via: "conditional check in startup() function"
      pattern: "ebay_app_id"
    - from: ".env.example"
      to: "app/config.py settings"
      via: "EBAY_APP_ID / EBAY_CERT_ID env vars loaded by pydantic-settings"
      pattern: "EBAY_(APP|CERT|DEV)_ID"
---

<objective>
Wire eBay credentials into operator-facing config and verify the adapter returns AU listings.

Purpose: The eBay adapter (`app/services/ebay.py`) is already implemented and registered, and real creds are already in `.env` locally. This phase delivers two things operators rely on: (1) `.env.example` documenting the eBay credentials so future setups know what to provide, and (2) a startup warning so absent credentials are visible rather than silently producing zero eBay results. Then verify that a live scan returns eBay listings.

Output: Updated `.env.example`, startup warning in `app/main.py`, and a verified scan showing eBay results in the UI/scan output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-ebay-credentials/25-CONTEXT.md
@app/main.py
@app/config.py
@app/services/ebay.py
@.env.example

<interfaces>
<!-- Key config + startup contracts the executor needs. Extracted from codebase. -->

From app/config.py (already present — do NOT add new fields):
```python
class Settings(BaseSettings):
    ebay_app_id: Optional[str] = None    # eBay App ID (Client ID) for Browse API
    ebay_cert_id: Optional[str] = None   # eBay Cert ID (Client Secret) for Browse API
```

From app/main.py (the startup() function — this is where the warning goes):
```python
@app.on_event("startup")
async def startup():
    async def _init_db_with_retries():
        for attempt in range(1, 6):
            try:
                await asyncio.to_thread(run_migrations)
                await asyncio.to_thread(Base.metadata.create_all, bind=engine)
                print("[startup] DB init complete")
                return
            except Exception as e:
                print(f"[startup] DB init attempt {attempt}/5 failed: {e}")
                await asyncio.sleep(min(30, 2 ** attempt))

        print("[startup] DB init failed after retries; app will continue and retry on demand")

    asyncio.create_task(_init_db_with_retries())
    setup_scheduler()
    scheduler.start()
```

From app/services/ebay.py (existing skip pattern — mirror the same boolean check in startup):
```python
if not settings.ebay_app_id or not settings.ebay_cert_id:
    return []
```

Existing `[startup]` log prefix convention: `print("[startup] ...")`.

Existing `.env.example` section convention:
```
# Description sentence of the credential and where to obtain it
KEY_NAME=placeholder_value
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document eBay credentials in .env.example and add startup warning</name>
  <files>.env.example, app/main.py</files>
  <read_first>
    - .env.example (see current format and section ordering)
    - app/main.py (lines 1-46 — current startup() function and import block)
    - app/config.py (confirm ebay_app_id and ebay_cert_id already exist — do NOT add new config fields)
    - app/services/ebay.py (confirm the adapter already uses settings.ebay_app_id / settings.ebay_cert_id)
    - .planning/phases/25-ebay-credentials/25-CONTEXT.md (decisions D-01 through D-04)
  </read_first>
  <action>
Per D-02 and D-04, append an eBay credentials block to `.env.example` after the existing `DISCOGS_TOKEN` line (or at the end of the file if that placement reads more naturally). The block MUST use the existing `# comment` + `KEY=placeholder` convention and MUST include all three keys — EBAY_APP_ID, EBAY_CERT_ID, EBAY_DEV_ID — with this exact content (or semantically identical wording; do not drop any line):

```
# eBay Browse API — create an app at developer.ebay.com > My Account > Keysets (Production)
# EBAY_APP_ID is the "App ID (Client ID)" — used for OAuth2 Browse API auth
EBAY_APP_ID=your_ebay_app_id_here
# EBAY_CERT_ID is the "Cert ID (Client Secret)" — used for OAuth2 Browse API auth
EBAY_CERT_ID=your_ebay_cert_id_here
# EBAY_DEV_ID is the "Dev ID" — documented for completeness; NOT currently consumed by the app (Trading API only)
EBAY_DEV_ID=your_ebay_dev_id_here
```

Per D-04 do NOT add an `ebay_dev_id` field to `app/config.py` or reference it anywhere in `app/`. Documentation-only.

Per D-03, edit `app/main.py` `startup()` (the function starting at line 28). Immediately before the `asyncio.create_task(_init_db_with_retries())` line (i.e. after the inner `_init_db_with_retries` function is defined but before it is scheduled, OR equivalently as the first statement of `startup()` before the inner function — pick whichever reads cleaner; both are acceptable), add a conditional warning that mirrors the existing adapter skip check:

```python
if not settings.ebay_app_id or not settings.ebay_cert_id:
    print("[startup] WARNING: eBay credentials missing (EBAY_APP_ID and/or EBAY_CERT_ID); eBay adapter will return no results until configured")
```

Use the existing `[startup]` prefix convention. Do NOT raise, do NOT exit — the app must still start (EBAY-01 success criterion #3). `settings` is already imported on line 8 (`from app.config import settings`), so no new imports needed.

Do NOT modify: `app/config.py`, `app/services/ebay.py`, `app/services/adapter.py`, or any other file. Do NOT reformat unrelated lines in the files you touch.
  </action>
  <verify>
    <automated>grep -q "^EBAY_APP_ID=" .env.example &amp;&amp; grep -q "^EBAY_CERT_ID=" .env.example &amp;&amp; grep -q "^EBAY_DEV_ID=" .env.example &amp;&amp; grep -q "ebay_app_id" app/main.py &amp;&amp; grep -q "\[startup\].*eBay" app/main.py &amp;&amp; python -c "from app.main import app; print('imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^EBAY_APP_ID=' .env.example` returns `1`
    - `grep -c '^EBAY_CERT_ID=' .env.example` returns `1`
    - `grep -c '^EBAY_DEV_ID=' .env.example` returns `1`
    - `grep -q 'developer.ebay.com' .env.example` succeeds (source URL referenced)
    - `grep -q 'ebay_dev_id' app/config.py` FAILS (must NOT be added — D-04)
    - `grep -q 'ebay_dev_id' app/services/ebay.py` FAILS (must NOT be added — D-04)
    - `grep -q 'if not settings.ebay_app_id or not settings.ebay_cert_id' app/main.py` succeeds
    - `grep -qE '\[startup\].*[Ww]arning.*eBay|\[startup\].*eBay.*missing' app/main.py` succeeds
    - `python -c "from app.main import app"` exits 0 (module imports cleanly with the edit)
    - `git diff --stat app/config.py` shows no changes (config.py untouched)
    - `git diff --stat app/services/` shows no changes (services untouched)
  </acceptance_criteria>
  <done>
    - `.env.example` documents EBAY_APP_ID, EBAY_CERT_ID, EBAY_DEV_ID with descriptions and source URL
    - `app/main.py` startup() prints a visible `[startup]` warning line when either eBay credential is missing, and the app still starts
    - No other files modified
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live scan returns eBay AU listings</name>
  <files>N/A (human verification — no files modified in this task)</files>
  <read_first>
    - .planning/REQUIREMENTS.md §eBay Activation (EBAY-02 acceptance criteria)
    - .planning/phases/25-ebay-credentials/25-CONTEXT.md (D-01 — adapter already registered)
  </read_first>
  <action>
Pause execution and hand off to the user for live verification. Present the "how-to-verify" steps verbatim to the user and wait for the resume-signal.
  </action>
  <what-built>
    Task 1 added eBay credential documentation to `.env.example` and a startup warning in `app/main.py`. The eBay adapter itself is pre-existing. With real credentials already in local `.env`, a live scan should now return eBay AU listings alongside Discogs and Shopify — satisfying EBAY-02.
  </what-built>
  <how-to-verify>
    1. Confirm local `.env` has real values for `EBAY_APP_ID` and `EBAY_CERT_ID` (not the `.env.example` placeholders).
    2. Start the app locally: `uvicorn app.main:app --reload --port 8000`.
    3. Confirm startup output does NOT include `[startup] WARNING: eBay credentials missing ...` (because creds are set).
    4. Open the dashboard at `http://localhost:8000/`. Pick an active wishlist item (or add a well-known title like "Kendrick Lamar DAMN").
    5. Trigger a scan for that item (via the scan button / endpoint the app exposes).
    6. Inspect the resulting listings — you should see at least one listing with `source` or `store` field indicating eBay AU alongside Discogs / Shopify results. Alternatively, check logs for `[eBay]` output lines indicating listings were returned.
    7. (Optional negative check) Temporarily comment out `EBAY_APP_ID` in `.env`, restart, confirm the startup warning now prints, and the app still starts.

    Report back what you saw: eBay listings present? How many? Any errors in logs?
  </how-to-verify>
  <verify>Human confirms eBay listings appeared in scan output for an active wishlist item.</verify>
  <done>User types "approved" — EBAY-02 satisfied.</done>
  <resume-signal>Type "approved" if eBay listings appeared in scan results (EBAY-02 satisfied). Otherwise describe what happened (no listings / errors / credential issue) so we can diagnose.</resume-signal>
</task>

</tasks>

<verification>
- `.env.example` contains all three EBAY_* keys with descriptive comments
- `app/main.py` prints a `[startup]` warning when either of `settings.ebay_app_id` / `settings.ebay_cert_id` is absent, without preventing startup
- `app/config.py` and `app/services/ebay.py` are UNCHANGED (D-04 — documentation-only for EBAY_DEV_ID; adapter is already complete per D-01)
- Human-verified: a live scan returns eBay AU listings for an active wishlist item
</verification>

<success_criteria>
- EBAY-01: credentials documented in `.env.example` and startup warning wired (task 1 passes acceptance criteria)
- EBAY-02: live scan returns eBay AU listings for active items (task 2 checkpoint approved)
- App starts cleanly with or without eBay credentials
</success_criteria>

<output>
After completion, create `.planning/phases/25-ebay-credentials/25-01-SUMMARY.md`
</output>

---
phase: 17-typography-overhaul
plan: 03
type: execute
wave: 3
depends_on: ["17-01", "17-02"]
files_modified: []
autonomous: false
requirements: [TYPO-01, TYPO-02, TYPO-03]

must_haves:
  truths:
    - "User confirms Gothic A1 renders for body text AND price digits on dashboard, item detail, and modals (TYPO-01)"
    - "User confirms item name appears visually larger and heavier than price on every dashboard card (TYPO-02)"
    - "User confirms hierarchy holds on item detail page and inside Add/Edit modals (TYPO-03)"
  artifacts: []
  key_links: []
---

<objective>
Human visual sign-off on TYPO-01, TYPO-02, TYPO-03. Font swap and hierarchy are CSS-only — only a real browser can confirm rendering and visual hierarchy.

Purpose: Close out Phase 17 with the user verifying the Gothic A1 font and inverted hierarchy land as intended.
Output: User approval recorded in plan SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-typography-overhaul/17-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification of typography overhaul</name>
  <read_first>
    - .planning/REQUIREMENTS.md (TYPO-01, TYPO-02, TYPO-03 acceptance lines)
    - .planning/phases/17-typography-overhaul/17-CONTEXT.md (D-01, D-05, D-09)
  </read_first>
  <what-built>
    Plan 01: Gothic A1 self-hosted in /static/fonts/ as Light/Regular/Medium woff2; @font-face block added; --font-sans set to "Gothic A1"; base.html preloads GothicA1-Regular.woff2.

    Plan 02: --text-title bumped 18px → 23px; --text-price dropped 28px → 17px; .text-title weight 400 → 450; .card-price + .text-price weight 600 → 350; .price-best weight 600 → 400.
  </what-built>
  <how-to-verify>
    Start the dev server (`uvicorn app.main:app --reload`) and open `http://localhost:8000`.

    1. **TYPO-01 — Font swap.** Open browser DevTools → Network → Fonts. Hard refresh.
       - Confirm `GothicA1-Regular.woff2` loads from `/static/fonts/`.
       - Confirm NO request for `Inter-VariableFont_opsz,wght.woff2`.
       - Inspect any card's title (h3) → Computed → font-family should resolve to "Gothic A1".
       - Inspect any card's price (span.card-price) → Computed → font-family should ALSO be "Gothic A1" (D-02: gothic font renders the digits too — they should NOT look like system Helvetica/Arial).

    2. **TYPO-02 — Card hierarchy.** Look at the dashboard grid.
       - On every card, the album name (h3) should be visibly LARGER than the price below it.
       - The name should also feel slightly HEAVIER (denser strokes) than the price.
       - The price should read as a supporting detail, not the visual hero.

    3. **TYPO-03 — Hierarchy holds beyond the card grid.**
       - Click into any item to load `/item/{id}`. The h1 item name (24px/700) should clearly outrank the listing prices in the table (17px/350).
       - Click "+ Add Item" to open the Add modal. Modal headings and form labels should render in Gothic A1 (no Inter, no system font).
       - Open Edit modal from item detail. Same check.

    4. **Bodoni untouched.** The "CRATE" nav brand mark (top-left) should still render in Bodoni Moda — unchanged from before Phase 17.

    Reply `approved` if all four checks pass, OR describe what's wrong (which surface, which check failed).
  </how-to-verify>
  <files>N/A — verification only, no files modified</files>
  <action>
    Start the dev server and walk through the four checks in <how-to-verify>. Wait for user response. Record approval (or failure details) in the plan SUMMARY.
  </action>
  <verify>
    <automated>echo "Manual verification — see how-to-verify section"</automated>
  </verify>
  <acceptance_criteria>
    - User reports `GothicA1-Regular.woff2` loaded and `Inter-VariableFont*.woff2` did NOT load (Network tab evidence)
    - User confirms price digits render in Gothic A1, not system numerals
    - User confirms name > price on every dashboard card
    - User confirms hierarchy on item detail page and in Add/Edit modals
    - User confirms CRATE nav brand still in Bodoni Moda
    - User responds `approved`
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the failing check.</resume-signal>
  <done>User responds approved; TYPO-01, TYPO-02, TYPO-03 all signed off and ready to mark complete in REQUIREMENTS.md traceability table.</done>
</task>

</tasks>

<verification>
Human visual confirmation of all three TYPO requirements across dashboard, item detail, and modals.
</verification>

<success_criteria>
Phase 17 complete: Gothic A1 swap delivered, name > price hierarchy applied, hierarchy holds across surfaces, no regressions to Bodoni Moda nav brand.
</success_criteria>

<output>
After completion, create `.planning/phases/17-typography-overhaul/17-03-SUMMARY.md` recording the user's approval and any followup notes.
</output>

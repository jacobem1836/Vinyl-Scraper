---
phase: 17-typography-overhaul
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - static/style.css
autonomous: true
requirements: [TYPO-02, TYPO-03]

must_haves:
  truths:
    - "On every dashboard card, the item name is visually larger and heavier than the price"
    - "--text-title is 23px (in the 22–24px range, D-05)"
    - "--text-price is 17px (in the 16–18px range, D-05)"
    - ".text-title weight is 450 (in the 400–500 range, D-05)"
    - ".card-price and .text-price weight is 350 (in the 300–400 range, D-05)"
    - "Hierarchy holds on item detail page (h1.text-heading 24px outranks .text-price 17px) and inside modals via inheritance"
  artifacts:
    - path: "static/style.css"
      provides: "Updated --text-title, --text-price tokens and .text-title / .card-price / .text-price utility weights"
      contains: "--text-title:             23px"
  key_links:
    - from: ".text-title (cards h3)"
      to: "--text-title (23px) + weight 450"
      via: "CSS custom property + utility class"
      pattern: "\\.text-title[\\s\\S]*?font-weight:\\s*450"
    - from: ".card-price (cards span)"
      to: "--text-price (17px) + weight 350"
      via: "CSS custom property + utility class"
      pattern: "\\.card-price[\\s\\S]*?font-weight:\\s*350"
---

<objective>
Invert the card hierarchy so item name (~23px / weight 450) reads as the visual hero and price (~17px / weight 350) becomes a supporting detail. Per D-05, D-06, D-07, D-08, D-09, D-10.

Purpose: Deliver TYPO-02 (name > price on cards) and TYPO-03 (hierarchy holds across dashboard, item detail, modals — font swap from plan 01 propagates everywhere via --font-sans).
Output: Updated typography tokens and utility-class weights in static/style.css.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-typography-overhaul/17-CONTEXT.md
@static/style.css
@templates/index.html
@templates/item_detail.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update --text-title and --text-price tokens for inverted hierarchy</name>
  <files>static/style.css</files>
  <read_first>
    - static/style.css (lines 91–103 — Typography token block)
    - .planning/phases/17-typography-overhaul/17-CONTEXT.md (D-05, D-06, D-07)
  </read_first>
  <action>
    Per D-05 and D-07, update the token VALUES in `:root` so the cascade flips name > price.

    EDIT — `static/style.css` lines 95–96 (current values):
    ```
    --text-title:             18px;   /* card album names — subordinate to artwork */
    --text-price:             28px;   /* core value signal */
    ```

    REPLACE WITH:
    ```
    --text-title:             23px;   /* card album names — primary visual hero (D-05/D-06) */
    --text-price:             17px;   /* supporting detail under the name (D-05/D-06) */
    ```

    Rationale on chosen values within the D-05 ranges (Claude's discretion per CONTEXT):
    - 23px sits in the upper half of the 22–24px name range — strong hierarchy without overpowering artwork
    - 17px sits in the middle of the 16–18px price range — clearly subordinate to 23px name

    Do NOT touch any other token in the block (--text-label, --text-sm, --text-body, --text-heading 24px, --text-subheading, --text-heading-secondary all unchanged). The comment block header "/* Typography — D-09 reverted scale */" can stay; do not rewrite it.
  </action>
  <verify>
    <automated>grep -E '^\s*--text-title:\s+23px' static/style.css && grep -E '^\s*--text-price:\s+17px' static/style.css && grep -E '^\s*--text-heading:\s+24px' static/style.css && grep -E '^\s*--text-body:\s+16px' static/style.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -- '--text-title:' static/style.css` shows value `23px`
    - `grep -- '--text-price:' static/style.css` shows value `17px`
    - `grep -- '--text-heading:' static/style.css` still shows `24px` (untouched — preserves item-detail h1 hierarchy per D-09)
    - `grep -- '--text-body:' static/style.css` still shows `16px` (untouched)
    - `grep -- '--text-subheading:' static/style.css` still shows `20px` (untouched)
    - No new tokens added; only the two specified lines changed
  </acceptance_criteria>
  <done>--text-title is 23px and --text-price is 17px in :root; all other typography tokens unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update .text-title / .card-price / .text-price utility-class font-weights to match D-05 ranges</name>
  <files>static/style.css</files>
  <read_first>
    - static/style.css (lines 532–551 — Typography utility classes)
    - .planning/phases/17-typography-overhaul/17-CONTEXT.md (D-05 weights: name 400–500, price 300–400)
  </read_first>
  <action>
    Per D-05, weights pair with the new sizes so the gothic font reads correctly:
    - Name (`.text-title`): weight 450 (mid-range of 400–500; Gothic A1 has 400 and 500 weights, browsers render 450 by interpolating to nearest available — falls back to 400 cleanly with light gothic feel)
    - Price (`.card-price`, `.text-price`): weight 350 (mid-range of 300–400; falls to nearest of Gothic A1 Light 300 / Regular 400)

    Note Gothic A1 is loaded as static weights 300/400/500 (not variable). Browsers will round 450 to 400 and 350 to 300 when matching @font-face rules. This is the intended thinness per D-05 and the gothic brief.

    EDIT 1 — `static/style.css` line 538 (current):
    `.text-title      { font-size: var(--text-title); font-weight: 400; line-height: 1.3; }`

    REPLACE WITH:
    `.text-title      { font-size: var(--text-title); font-weight: 450; line-height: 1.25; }`

    EDIT 2 — `static/style.css` line 541 (current):
    `.text-price      { font-size: var(--text-price); font-weight: 600; line-height: 1.2; }`

    REPLACE WITH:
    `.text-price      { font-size: var(--text-price); font-weight: 350; line-height: 1.3; }`

    EDIT 3 — `static/style.css` line 542 (current):
    `.card-price      { font-size: var(--text-price); font-weight: 600; line-height: 1.2; }`

    REPLACE WITH:
    `.card-price      { font-size: var(--text-price); font-weight: 350; line-height: 1.3; }`

    EDIT 4 — `static/style.css` line 550 (current):
    `.price-best { color: var(--color-accent); font-weight: 600; }`

    REPLACE WITH:
    `.price-best { color: var(--color-accent); font-weight: 400; }`

    Rationale: `.price-best` is composed onto `.card-price` (see templates/index.html line 187: `class="card-price price-best"`) — keeping it at 600 would override the new 350 and undo the hierarchy. 400 keeps it slightly heavier than non-best prices while staying within D-05's 300–400 price range.

    Do NOT modify .text-heading (24px / 700 — needed for item detail h1 per D-09), .text-subheading, .text-label, .text-sm, .text-body, or any non-typography utility.
  </action>
  <verify>
    <automated>grep -E '\.text-title\s+\{[^}]*font-weight:\s*450' static/style.css && grep -E '\.card-price\s+\{[^}]*font-weight:\s*350' static/style.css && grep -E '\.text-price\s+\{[^}]*font-weight:\s*350' static/style.css && grep -E '\.price-best\s+\{[^}]*font-weight:\s*400' static/style.css && grep -E '\.text-heading\s+\{[^}]*font-weight:\s*700' static/style.css</automated>
  </verify>
  <acceptance_criteria>
    - `.text-title` rule contains `font-weight: 450`
    - `.card-price` rule contains `font-weight: 350`
    - `.text-price` rule contains `font-weight: 350`
    - `.price-best` rule contains `font-weight: 400` (no longer 600 — would otherwise override card-price weight)
    - `.text-heading` rule still contains `font-weight: 700` (untouched per D-09)
    - `.text-subheading` rule still contains `font-weight: 600` (untouched)
    - No template files modified — change is CSS-only (D-07, D-08)
  </acceptance_criteria>
  <done>.text-title weighs 450, .card-price and .text-price weigh 350, .price-best weighs 400, and .text-heading remains at 700. Hierarchy now reads name > price on dashboard cards and modals; item-detail h1 (24px/700) outranks listing prices (17px/350).</done>
</task>

</tasks>

<verification>
- `--text-title` = 23px and `--text-price` = 17px in :root
- `.text-title` font-weight 450; `.card-price` and `.text-price` font-weight 350; `.price-best` font-weight 400
- `.text-heading` and `.text-subheading` unchanged (item detail + modals retain their structural hierarchy)
- Manual: load dashboard, item detail, and the Add Item modal — confirm name visibly outweighs price on each card and detail view, gothic font renders for both letters and digits
</verification>

<success_criteria>
TYPO-02: dashboard cards show item name larger and heavier than price.
TYPO-03: hierarchy holds on item detail (h1 24px > listing prices 17px) and inside modals (font swap propagates via --font-sans, no per-template overrides needed).
</success_criteria>

<output>
After completion, create `.planning/phases/17-typography-overhaul/17-02-SUMMARY.md`
</output>

---
phase: 17-typography-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/fonts/GothicA1-Light.woff2
  - static/fonts/GothicA1-Regular.woff2
  - static/fonts/GothicA1-Medium.woff2
  - static/style.css
  - templates/base.html
autonomous: true
requirements: [TYPO-01]

must_haves:
  truths:
    - "Gothic A1 woff2 files exist in /static/fonts/ (Light 300, Regular 400, Medium 500)"
    - "@font-face declares 'Gothic A1' for weights 300, 400, 500 with font-display: swap"
    - "--font-sans starts with 'Gothic A1' instead of 'Inter'"
    - "base.html preloads the Gothic A1 Regular woff2 (Inter preload removed)"
    - "Body text and price digits render in Gothic A1 across dashboard, item detail, and modals"
  artifacts:
    - path: "static/fonts/GothicA1-Light.woff2"
      provides: "Gothic A1 weight 300 self-hosted"
    - path: "static/fonts/GothicA1-Regular.woff2"
      provides: "Gothic A1 weight 400 self-hosted"
    - path: "static/fonts/GothicA1-Medium.woff2"
      provides: "Gothic A1 weight 500 self-hosted"
    - path: "static/style.css"
      provides: "@font-face for Gothic A1 + --font-sans updated"
      contains: "Gothic A1"
    - path: "templates/base.html"
      provides: "Preload link pointing to Gothic A1 Regular"
      contains: "GothicA1-Regular.woff2"
  key_links:
    - from: "static/style.css :root"
      to: "static/fonts/GothicA1-*.woff2"
      via: "@font-face url()"
      pattern: "@font-face[\\s\\S]*?Gothic A1[\\s\\S]*?GothicA1-"
    - from: "templates/base.html"
      to: "static/fonts/GothicA1-Regular.woff2"
      via: "<link rel=preload>"
      pattern: "preload.*GothicA1-Regular\\.woff2"
---

<objective>
Replace Inter with self-hosted Gothic A1 (Light/Regular/Medium) as the body typeface across the app. Per D-01, D-02, D-03, D-04 — Bodoni Moda untouched.

Purpose: Deliver TYPO-01 — thinner, crispier gothic typeface for cards, body text, and price numerals.
Output: Three Gothic A1 woff2 files self-hosted, @font-face block + --font-sans updated, preload link swapped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-typography-overhaul/17-CONTEXT.md
@static/style.css
@templates/base.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download Gothic A1 woff2 files (Light 300, Regular 400, Medium 500) into static/fonts/</name>
  <files>static/fonts/GothicA1-Light.woff2, static/fonts/GothicA1-Regular.woff2, static/fonts/GothicA1-Medium.woff2</files>
  <read_first>
    - static/fonts/ (verify directory contents — confirm Inter and BodoniModa already self-hosted as woff2)
  </read_first>
  <action>
    Per D-01 and D-03, source Gothic A1 from Google Fonts and self-host as woff2. Gothic A1 has Latin coverage including full numerals (D-02 requires gothic font for price digits).

    Steps:
    1. Fetch the Gothic A1 CSS from Google Fonts API to discover current woff2 URLs:
       `curl -s -A "Mozilla/5.0" "https://fonts.googleapis.com/css2?family=Gothic+A1:wght@300;400;500&display=swap" -o /tmp/gothica1.css`
    2. Inspect /tmp/gothica1.css and extract the three latin-subset woff2 URLs (look for the `/* latin */` block, NOT korean or latin-ext).
    3. Download each and save with these EXACT filenames:
       - Weight 300 → `static/fonts/GothicA1-Light.woff2`
       - Weight 400 → `static/fonts/GothicA1-Regular.woff2`
       - Weight 500 → `static/fonts/GothicA1-Medium.woff2`
    4. Verify each file is non-zero bytes and starts with the woff2 magic header (`wOF2` = bytes `77 4F 46 32`):
       `head -c 4 static/fonts/GothicA1-Regular.woff2 | xxd`

    Do NOT use a CDN link. Do NOT keep variable-font format — three static weights only (D-03 explicit).
  </action>
  <verify>
    <automated>test -s static/fonts/GothicA1-Light.woff2 && test -s static/fonts/GothicA1-Regular.woff2 && test -s static/fonts/GothicA1-Medium.woff2 && head -c 4 static/fonts/GothicA1-Regular.woff2 | grep -q wOF2</automated>
  </verify>
  <acceptance_criteria>
    - `ls static/fonts/GothicA1-Light.woff2 static/fonts/GothicA1-Regular.woff2 static/fonts/GothicA1-Medium.woff2` lists all three
    - Each file is > 0 bytes
    - `head -c 4 static/fonts/GothicA1-Regular.woff2` outputs `wOF2` (woff2 magic bytes)
  </acceptance_criteria>
  <done>Three Gothic A1 woff2 files exist in static/fonts/, all non-empty, all valid woff2 magic header.</done>
</task>

<task type="auto">
  <name>Task 2: Add Gothic A1 @font-face block, update --font-sans, swap preload link</name>
  <files>static/style.css, templates/base.html</files>
  <read_first>
    - static/style.css (lines 1–110 — current @font-face declarations and :root tokens)
    - templates/base.html (lines 1–20 — current preload links)
    - .planning/phases/17-typography-overhaul/17-CONTEXT.md (D-01 through D-04)
  </read_first>
  <action>
    Per D-01, D-02, D-03, D-04 — replace Inter with Gothic A1 as body typeface; Bodoni Moda @font-face stays untouched.

    EDIT 1 — `static/style.css` lines 18–24 (existing Inter @font-face block):

    REPLACE the entire Inter @font-face block with three Gothic A1 @font-face declarations:

    ```css
    @font-face {
      font-family: "Gothic A1";
      src: url("fonts/GothicA1-Light.woff2") format("woff2");
      font-weight: 300;
      font-style: normal;
      font-display: swap;
    }

    @font-face {
      font-family: "Gothic A1";
      src: url("fonts/GothicA1-Regular.woff2") format("woff2");
      font-weight: 400;
      font-style: normal;
      font-display: swap;
    }

    @font-face {
      font-family: "Gothic A1";
      src: url("fonts/GothicA1-Medium.woff2") format("woff2");
      font-weight: 500;
      font-style: normal;
      font-display: swap;
    }
    ```

    Note `src: url("fonts/...")` is RELATIVE — style.css lives in /static/, fonts live in /static/fonts/, so relative path matches the existing Bodoni Moda pattern. Do NOT prefix with `/static/`.

    EDIT 2 — `static/style.css` line 101 (--font-sans):

    REPLACE:
    `--font-sans: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;`

    WITH:
    `--font-sans: "Gothic A1", -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;`

    Bodoni Moda block (lines 10–16) and `--font-display` (line 102) are UNTOUCHED (D-04).

    EDIT 3 — `templates/base.html` line 8 (preload link):

    REPLACE:
    `<link rel="preload" href="/static/fonts/Inter-VariableFont_opsz,wght.woff2" as="font" type="font/woff2" crossorigin>`

    WITH:
    `<link rel="preload" href="/static/fonts/GothicA1-Regular.woff2" as="font" type="font/woff2" crossorigin>`

    Only Regular (400) is preloaded — Light and Medium load on demand. Bodoni preload (line 7) UNTOUCHED.

    Do NOT delete the old Inter woff2 file in this task — leave it in place; cleanup is out of scope. Just ensure no CSS or HTML references it.
  </action>
  <verify>
    <automated>grep -q 'font-family: "Gothic A1"' static/style.css && grep -q 'GothicA1-Regular.woff2' static/style.css && grep -q 'GothicA1-Light.woff2' static/style.css && grep -q 'GothicA1-Medium.woff2' static/style.css && grep -q '"Gothic A1", -apple-system' static/style.css && grep -q 'GothicA1-Regular.woff2' templates/base.html && ! grep -q 'Inter-VariableFont' templates/base.html && ! grep -q 'font-family: "Inter"' static/style.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'font-family: "Gothic A1"' static/style.css` returns `3` (three weight declarations)
    - `grep 'src: url("fonts/GothicA1' static/style.css` shows three matches with relative `fonts/` paths (no leading slash)
    - `grep -- '--font-sans:' static/style.css` shows line beginning with `"Gothic A1",`
    - `grep 'font-family: "Inter"' static/style.css` returns nothing (Inter @font-face removed)
    - `grep -- '--font-display' static/style.css` still references `"Bodoni Moda"` (untouched)
    - `grep 'preload' templates/base.html | grep -c 'GothicA1-Regular.woff2'` returns `1`
    - `grep 'preload' templates/base.html | grep 'Inter-VariableFont'` returns nothing
    - Bodoni Moda preload line in base.html (line 7) is unchanged: `grep 'BodoniModa-Bold.woff2' templates/base.html` still matches
  </acceptance_criteria>
  <done>style.css declares three Gothic A1 weights (300/400/500), --font-sans points at Gothic A1, base.html preloads GothicA1-Regular.woff2, and no references to Inter remain in either file.</done>
</task>

</tasks>

<verification>
- All three Gothic A1 woff2 files present in static/fonts/
- style.css contains exactly three `font-family: "Gothic A1"` @font-face blocks
- --font-sans first family is "Gothic A1"
- base.html preloads GothicA1-Regular.woff2 (no Inter preload)
- Bodoni Moda untouched (D-04)
</verification>

<success_criteria>
TYPO-01 partially delivered: gothic typeface loaded and applied site-wide via --font-sans. Hierarchy work (TYPO-02/03) follows in plan 02.
</success_criteria>

<output>
After completion, create `.planning/phases/17-typography-overhaul/17-01-SUMMARY.md`
</output>

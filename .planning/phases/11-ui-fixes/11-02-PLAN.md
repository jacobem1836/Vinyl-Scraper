---
phase: 11-ui-fixes
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - static/style.css
  - templates/index.html
autonomous: true
requirements: []
must_haves:
  truths:
    - "Deal badge is invisible at rest, fades in on card hover"
    - "Deal cards have no coloured left border at rest"
    - "Ghost cards (no listings) are at 0.35 opacity with no border"
    - "Cards fill more viewport width with reduced side margins"
    - "Cards lift 2px with shadow on hover"
    - "Buttons press down to scale(0.97) on :active"
  artifacts:
    - path: "static/style.css"
      provides: "Card tier overrides, hover transitions, scrollbar, interaction states"
      contains: "card-deal-badge"
    - path: "templates/index.html"
      provides: "Updated card grid wrapper with reduced padding"
  key_links:
    - from: "static/style.css .card:hover"
      to: ".card-deal-badge"
      via: "parent hover reveals child badge"
      pattern: "\\.card:hover .card-deal-badge"
---

<objective>
Implement card system overhaul (deal badge hover, ghost cards, margins) and interaction layer (hover effects, micro-interactions, transitions).

Purpose: Transform the dashboard from always-coloured to B&W-at-rest with colour-on-engagement. Add the refined-yet-brutalist interaction feel.
Output: Updated card classes, hover states, and micro-interactions in CSS + minor template adjustment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-ui-fixes/11-CONTEXT.md
@.planning/phases/11-ui-fixes/11-UI-SPEC.md
@.planning/phases/11-ui-fixes/11-01-SUMMARY.md
@static/style.css
@templates/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Card system — deal badge hover, ghost cards, margins</name>
  <files>static/style.css, templates/index.html</files>
  <action>
**CSS changes in `static/style.css`** (per D-06, D-07, D-08):

1. **Deal badge hidden at rest** (D-06): Update `.card-deal-badge` to add:
   ```
   opacity: 0;
   visibility: hidden;
   transition: opacity 150ms ease-out, visibility 150ms ease-out;
   ```
   Add new rule `.card:hover .card-deal-badge`:
   ```
   opacity: 1;
   visibility: visible;
   ```

2. **Remove deal card left border** (D-06): Change `.card--deal` from `border-left: 3px solid var(--color-success)` to `border-left: none` (or remove the rule entirely).

3. **Ghost card styling** (D-07): Update `.card--empty`:
   - `opacity: 0.5` -> `opacity: 0.35`
   - `border-style: dashed` -> `border: none`
   
4. **Reduce card grid side margins** (D-08): In `.container`, keep `padding-inline: var(--space-md)`. But add a new rule for the card grid section specifically. In `templates/index.html`, add class `card-grid-container` to the `<section class="card-grid">` wrapper. In CSS, add:
   ```
   .card-grid-container {
     margin-inline: calc(-1 * var(--space-sm));
   }
   ```
   This pulls the grid closer to edges within the existing container. Alternatively — and simpler — reduce the `.container` `padding-inline` to `var(--space-sm)` only when it wraps the card grid. The cleanest approach: in `templates/index.html`, change the card grid `<section>` to have `style="margin-inline: calc(-1 * var(--space-sm));"` to pull it outward by 8px on each side.

**Template change in `templates/index.html`**:
- On the `<section class="card-grid">` element (line ~144), add `style="margin-inline: calc(-1 * var(--space-sm));"` to reduce effective side margins (D-08).

Do NOT use Magic MCP for this task — card system changes are fully specified in UI-SPEC.
  </action>
  <verify>
    <automated>grep -c "card:hover .card-deal-badge" static/style.css | xargs test 0 -lt</automated>
  </verify>
  <done>Deal badge hidden at rest, visible on hover. Ghost cards at 0.35 opacity with no border. Card grid uses more viewport width.</done>
</task>

<task type="auto">
  <name>Task 2: Interaction layer — hover effects, micro-interactions, transitions</name>
  <files>static/style.css</files>
  <action>
**CSS changes in `static/style.css`** (per D-12, D-13, D-14):

1. **Card hover** (UI-SPEC Interaction Contract): Update `.card` transition from `transition: transform 200ms ease, box-shadow 200ms ease` to `transition: transform 150ms ease-out, box-shadow 150ms ease-out`. Update `.card:hover`:
   - `transform: translateY(-2px)` (keep)
   - `box-shadow: 0 4px 16px rgba(0,0,0,0.4)` (replace current heavy shadow with this subtler one)

2. **Button hover states for B&W-on-hover colour** (D-04):
   - `.btn-destructive:hover`: change from `background: var(--color-destructive-bg)` to `background: var(--color-destructive); color: #ffffff; border-color: var(--color-destructive)` (muted red bg, white text on hover)
   - `.btn-secondary:hover`: change from `background: var(--color-surface)` to `background: #ffffff; color: #0d0b0a` (white bg, dark text on hover per UI-SPEC)

3. **Button transitions**: Update `.btn-cta`, `.btn-secondary`, `.btn-destructive` transitions from `transition: background 200ms` to `transition: background 150ms, color 150ms`.

4. **Button :active** (already exists as `transform: scale(0.97)` — verify it's correct). The UI-SPEC says "instant cut in, ease-out on release". Add `transition: transform 0ms` on `:active` state, and ensure the base button has `transition: ... , transform 150ms ease-out` so the release animates. Implementation:
   - On base `.btn-cta` / `.btn-secondary` / `.btn-destructive`: add `transform 150ms ease-out` to existing transition.
   - On the shared `:active` rule: add `transition-duration: 0ms` so the scale is instant on press.

5. **Scrollbar styling** (D-17): Add at end of style.css:
   ```css
   /* Scrollbar — B&W palette (D-17) */
   ::-webkit-scrollbar {
     width: 6px;
   }
   ::-webkit-scrollbar-track {
     background: #1a1a1a;
   }
   ::-webkit-scrollbar-thumb {
     background: #444444;
     border-radius: 3px;
   }
   ::-webkit-scrollbar-thumb:hover {
     background: #666666;
   }
   
   /* Firefox scrollbar */
   * {
     scrollbar-width: thin;
     scrollbar-color: #444444 #1a1a1a;
   }
   ```

Note on D-13 (Magic MCP): The interaction contract is fully specified in UI-SPEC. Magic MCP should be consulted if the executor wants to explore additional micro-interactions beyond what's specified, but all required interactions are documented above.
  </action>
  <verify>
    <automated>grep -c "webkit-scrollbar" static/style.css | xargs test 0 -lt</automated>
  </verify>
  <done>Card hover transitions at 150ms ease-out. Button hovers show colour only on interaction. Scrollbar styled B&W at 6px width. :active is instant scale(0.97) with ease-out release.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — CSS and template layout changes only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | n/a | CSS interactions | accept | Purely visual changes |
</threat_model>

<verification>
- Hover over a deal card: badge should fade in, card lifts 2px
- Deal cards at rest: no coloured left border, no visible badge
- Ghost cards (no listings): very faded (35% opacity), no border
- Card grid uses more of the viewport (less side padding)
- Scroll the page: scrollbar is 6px wide, dark grey
- Type in typeahead to get dropdown: scrollbar matches B&W style
- Click a button: instant press-down effect, smooth release
- Hover destructive button: muted red background appears
</verification>

<success_criteria>
- Deal badge opacity transitions from 0 to 1 on card hover
- No colour visible on cards at rest (B&W gallery wall)
- Scrollbar is 6px, dark track, grey thumb
- All button transitions use 150ms timing
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-fixes/11-02-SUMMARY.md`
</output>

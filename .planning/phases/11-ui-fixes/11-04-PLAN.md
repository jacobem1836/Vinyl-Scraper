---
phase: 11-ui-fixes
plan: 04
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - templates/base.html
  - static/style.css
  - static/logo.svg
autonomous: false
requirements: []
must_haves:
  truths:
    - "User has selected a logo from multiple AI-generated options"
    - "User has selected a display font from multiple options"
    - "Logo appears as 28x28 SVG left of CRATE wordmark in nav"
    - "CRATE wordmark uses the user-chosen display font"
    - "Logo and font are self-hosted (no external CDN)"
  artifacts:
    - path: "static/logo.svg"
      provides: "Chosen logo SVG file"
    - path: "templates/base.html"
      provides: "Logo img tag in nav + font preload"
    - path: "static/style.css"
      provides: "Updated @font-face and --font-display"
  key_links:
    - from: "templates/base.html .nav-brand"
      to: "static/logo.svg"
      via: "img src in nav"
      pattern: "logo.svg"
---

<objective>
Present logo and font options to user, then implement the selected choices in the nav bar.

Purpose: Establish the CRATE visual brand identity with a custom logo icon and brutalist display font.
Output: Logo SVG, updated font, both wired into base.html nav.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-ui-fixes/11-CONTEXT.md
@.planning/phases/11-ui-fixes/11-UI-SPEC.md
@.planning/phases/11-ui-fixes/11-01-SUMMARY.md
@templates/base.html
@static/style.css
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Logo selection</name>
  <decision>Select logo for CRATE brand identity (D-01, D-02)</decision>
  <context>
The logo concept is "vinyl on a shelf from side on" -- abstract diagonal lines evoking records standing in a crate. Must be:
- Monochrome SVG (white on dark bg)
- 28x28px display size
- Abstract/geometric -- not a literal record player or turntable
- Fits brutalist B&W aesthetic

Generate 3-4 SVG logo options using AI tooling (code-generated inline SVGs). Each should explore a different interpretation of "diagonal lines / records in a crate":
1. Tight parallel diagonal lines at ~15 degree tilt (like vinyl spines viewed from above)
2. Stacked angular rectangles fanning out (records leaning in a crate)
3. Minimal crate outline with diagonal line fills
4. Abstract V-shape formed from converging lines (vinyl + crate)

Present each as an inline SVG rendered at 28x28px with a dark (#0d0b0a) background preview.
  </context>
  <options>
    <option id="option-spines">
      <name>Parallel spines</name>
      <pros>Minimal, distinctive, scales well</pros>
      <cons>May read as generic lines at small size</cons>
    </option>
    <option id="option-fan">
      <name>Fanned records</name>
      <pros>Clearly evokes vinyl collection</pros>
      <cons>More complex SVG</cons>
    </option>
    <option id="option-crate">
      <name>Crate outline with fills</name>
      <pros>Direct brand connection</pros>
      <cons>May be too literal</cons>
    </option>
    <option id="option-abstract">
      <name>Abstract V-lines</name>
      <pros>Unique, memorable</pros>
      <cons>Less obviously vinyl-related</cons>
    </option>
  </options>
  <resume-signal>Select one option or request modifications</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Font selection</name>
  <decision>Select brutalist display font for CRATE wordmark (D-10)</decision>
  <context>
Current font: Bodoni Moda Bold (self-hosted woff2 at static/fonts/). Must fit brutalist B&W aesthetic. Used ONLY for the CRATE nav wordmark (24-28px). System font stack used for all other text.

Present 3-5 options with rationale. Each should be available as a free/open-source woff2. Consider:
1. **Bodoni Moda Bold** (current) -- high contrast serif, elegant brutalism
2. **Space Mono Bold** -- monospace, technical, raw
3. **Archivo Black** -- wide grotesque, bold presence
4. **Bebas Neue** -- condensed uppercase, poster-like
5. **DM Mono** -- geometric monospace, clean brutalism

For each option, describe the visual character and show how "CRATE" would feel in that typeface at the nav size (letter-spacing: 0.25em, uppercase).
  </context>
  <options>
    <option id="option-bodoni">
      <name>Bodoni Moda Bold (keep current)</name>
      <pros>Already self-hosted, elegant high-contrast, distinctive</pros>
      <cons>More classical than brutalist, may feel precious</cons>
    </option>
    <option id="option-space-mono">
      <name>Space Mono Bold</name>
      <pros>Monospace = technical/raw, great at wide letter-spacing</pros>
      <cons>Less impactful at small size</cons>
    </option>
    <option id="option-archivo">
      <name>Archivo Black</name>
      <pros>Wide, commanding presence, very legible</pros>
      <cons>Common choice, less unique</cons>
    </option>
    <option id="option-bebas">
      <name>Bebas Neue</name>
      <pros>Condensed uppercase feels poster-like, brutalist energy</pros>
      <cons>Overused in web design</cons>
    </option>
    <option id="option-dm-mono">
      <name>DM Mono</name>
      <pros>Geometric, clean, modern brutalism</pros>
      <cons>May lack the weight needed for a wordmark</cons>
    </option>
  </options>
  <resume-signal>Select a font or request more options</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Implement selected logo and font</name>
  <files>static/logo.svg, static/style.css, templates/base.html</files>
  <action>
After user selects logo and font:

1. **Logo**: Save the selected SVG to `static/logo.svg`. Ensure viewBox is set for crisp rendering at 28x28.

2. **Font**: If user selects a new font (not Bodoni Moda):
   - Download the woff2 file to `static/fonts/`
   - Update `@font-face` in `static/style.css` to reference the new font file
   - Update `--font-display` custom property
   - Update `font-display: block` to `font-display: swap` (per UI-SPEC — avoids blank text flash)
   - Update the `<link rel="preload">` in `templates/base.html` to reference the new font file

3. **Nav brand update in `templates/base.html`**: Change line 13 from:
   ```html
   <a href="/" class="nav-brand">CRATE</a>
   ```
   To:
   ```html
   <a href="/" class="nav-brand">
     <img src="/static/logo.svg" alt="" width="28" height="28" style="margin-right: var(--space-sm); vertical-align: middle;">CRATE
   </a>
   ```

4. **Nav brand CSS** in `static/style.css`: Update `.nav-brand` to add `display: inline-flex; align-items: center; gap: var(--space-sm);` and remove the inline `margin-right` from the img tag (use gap instead).
  </action>
  <verify>
    <automated>grep -c "logo.svg" templates/base.html | xargs test 0 -lt</automated>
  </verify>
  <done>Logo SVG appears 28x28 left of CRATE wordmark. Display font is the user's selection, self-hosted. No external CDN dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — static assets served from /static/.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | n/a | Static assets | accept | Self-hosted SVG and font, no external resources |
</threat_model>

<verification>
- Logo renders at 28x28 in nav, vertically centred with CRATE wordmark
- CRATE wordmark uses the selected display font
- No FOUT (font flash) on page load (preload + font-display: swap/block)
- Logo and font load from /static/ (no external CDN)
- Logo renders as white on the warm dark background
</verification>

<success_criteria>
- User approved logo and font before implementation
- Logo SVG exists at static/logo.svg
- Font is self-hosted at static/fonts/
- Nav displays logo + CRATE wordmark in chosen font
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-fixes/11-04-SUMMARY.md`
</output>

---
phase: 16
plan: 01
title: True black palette, skeleton pulse, and scrollbar rework
type: execute
wave: 1
depends_on: []
files_modified:
  - static/style.css
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03]
must_haves:
  truths:
    - "Dashboard, item detail, and modal surfaces render on #000 (no near-black fallbacks)"
    - "Scrollbars (global, typeahead dropdown, table container) use custom 4px track/thumb styling"
    - "Skeleton pulse on .card-artwork-wrapper remains visible against #000 background"
    - "Firefox scrollbar-color matches WebKit thumb/track values"
    - "CTA button text remains readable (dark on white CTA)"
  artifacts:
    - path: "static/style.css"
      provides: "Updated :root tokens, skeleton pulse values, scrollbar rework"
      contains: "--color-bg: #000000"
  key_links:
    - from: "static/style.css :root"
      to: "all templates consuming --color-bg / --color-surface / --color-surface-alt"
      via: "CSS custom property cascade"
      pattern: "--color-bg:\\s*#000000"
    - from: "static/style.css section 19"
      to: ".typeahead-dropdown and .table-container scrollbars"
      via: "scoped ::-webkit-scrollbar selectors"
      pattern: "\\.typeahead-dropdown::-webkit-scrollbar"
---

<objective>
Apply the Phase 16 visual foundation change: shift `--color-bg`, `--color-surface`, `--color-surface-alt`, and `--color-cta-text` to `#000000` (D-01), update the `.card-artwork-wrapper` skeleton pulse gradient so animation stays visible on pure black (D-03), and fully rework the scrollbar styling to a 4px transparent track with `rgba(255,255,255,0.2)` thumb / `rgba(255,255,255,0.4)` hover, scoped to global + `.typeahead-dropdown` + `.table-container`, with matching Firefox `scrollbar-color` (D-04, D-05, D-06).

Purpose: Establish the Warner Music–inspired true black foundation (VIS-01, VIS-03) and the custom scrollbar treatment (VIS-02) that later phases (typography, consistency fixes) build on.

Output: A single modified `static/style.css` — no template edits, no JS, no font changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-visual-foundation/16-CONTEXT.md
@.planning/phases/16-visual-foundation/16-UI-SPEC.md
@static/style.css
</context>

<tasks>

<task id="1" type="auto" title="Update :root color tokens to #000000" blocking="true">
  <name>Task 1: Shift --color-bg, --color-surface, --color-surface-alt, --color-cta-text to #000000</name>
  <files>static/style.css</files>
  <read_first>
    - static/style.css — read the entire `:root` block (section 1) to see current token values and adjacent tokens; confirm `--color-border: #222222` and `--color-border-muted: #111111` are present
    - .planning/phases/16-visual-foundation/16-UI-SPEC.md — §Color "Phase 16 Changes — What Moves" table is the source of truth for before/after values
    - .planning/phases/16-visual-foundation/16-CONTEXT.md — §Implementation Decisions D-01, D-02, D-08 (restraint — do not touch spacing or radius)
  </read_first>
  <action>
    In `static/style.css` within the `:root` block (section 1):
    - Change `--color-bg` from `#0d0b0a` to `#000000` (per D-01 / UI-SPEC Color table)
    - Change `--color-surface` from `#141210` to `#000000` (per D-01)
    - Change `--color-surface-alt` from `#0d0b0a` to `#000000` (per D-01)
    - Change `--color-cta-text` from `#0d0b0a` to `#000000` (per D-01 — keeps CTA text readable on the white `--color-accent-cta` button)

    Review `--color-border-muted: #111111`. If it visually disappears against `#000000` during the Wave 2 verification, raise to `#1a1a1a` (Claude discretion per CONTEXT.md §Claude's Discretion). If uncertain, leave at `#111111` for now and flag in the SUMMARY for the Wave 2 human-verify task.

    DO NOT touch: spacing tokens, border-radius tokens, `--color-text*`, `--color-accent*`, `--color-destructive*`, `--color-success`, `--color-border` (D-02/D-08 — restraint).
  </action>
  <verify>
    <automated>grep -nE "--color-(bg|surface|surface-alt|cta-text):\s*#000000" static/style.css | wc -l | awk '{ if ($1 >= 4) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `--color-bg: #000000` present in `:root` block
    - [ ] `--color-surface: #000000` present in `:root` block
    - [ ] `--color-surface-alt: #000000` present in `:root` block
    - [ ] `--color-cta-text: #000000` present in `:root` block
    - [ ] Previous values (`#0d0b0a`, `#141210`) do NOT appear anywhere in the `:root` block for these four tokens
    - [ ] `--color-border: #222222` is UNCHANGED (D-02)
    - [ ] Spacing scale tokens (`--space-*`) and border-radius tokens (`--radius-*`) are UNCHANGED (D-08)
    - [ ] `--color-accent: #ffffff` is UNCHANGED (D-07)
  </acceptance_criteria>
  <done>Four tokens updated to `#000000`; all other `:root` tokens untouched; file still parses as valid CSS.</done>
</task>

<task id="2" type="auto" title="Update .card-artwork-wrapper skeleton pulse gradient" blocking="true">
  <name>Task 2: Shift skeleton pulse hardcoded hex from #1a1a1a/#2a2a2a to #0a0a0a/#141414</name>
  <files>static/style.css</files>
  <read_first>
    - static/style.css — locate `.card-artwork-wrapper` skeleton rule in section 15; identify the `linear-gradient(...)` containing `#1a1a1a` and `#2a2a2a`
    - .planning/phases/16-visual-foundation/16-UI-SPEC.md — §Color "Skeleton Pulse (hardcoded, not tokens)" table for exact before/after values
    - .planning/phases/16-visual-foundation/16-CONTEXT.md — D-03 rationale ("slightly lifted from #000 so the animation is still visible")
  </read_first>
  <action>
    In `static/style.css` section 15, within the `.card-artwork-wrapper` skeleton pulse `background: linear-gradient(...)` declaration:
    - Replace `#1a1a1a` with `#0a0a0a`
    - Replace `#2a2a2a` with `#141414`

    These are hardcoded hex values, NOT CSS variables — do a precise textual replacement within the `.card-artwork-wrapper` rule only. Do NOT globally replace `#1a1a1a` or `#2a2a2a` across the file; other rules may use these values for different purposes.

    Rationale (D-03): `#000` on `#000` is invisible. `#0a0a0a` / `#141414` are the minimum lift needed for the pulse animation to remain perceptible on the new true-black surface.
  </action>
  <verify>
    <automated>grep -nE "#0a0a0a|#141414" static/style.css | grep -i "artwork\|skeleton" || grep -cE "#0a0a0a|#141414" static/style.css | awk '{ if ($1 >= 2) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `.card-artwork-wrapper` skeleton gradient contains `#0a0a0a`
    - [ ] `.card-artwork-wrapper` skeleton gradient contains `#141414`
    - [ ] `.card-artwork-wrapper` skeleton gradient does NOT contain `#1a1a1a` or `#2a2a2a`
    - [ ] Replacement is scoped to `.card-artwork-wrapper` — other rules in section 15+ are untouched
    - [ ] Animation keyframes / timing / `animation:` property are UNCHANGED
  </acceptance_criteria>
  <done>Skeleton pulse colors lifted to `#0a0a0a` / `#141414` within `.card-artwork-wrapper` only; no collateral edits to other rules.</done>
</task>

<task id="3" type="auto" title="Full scrollbar rework (WebKit + Firefox, global + scoped)" blocking="true">
  <name>Task 3: Replace section 19 scrollbar styling with 4px transparent-track / white-translucent-thumb, scoped to global + .typeahead-dropdown + .table-container</name>
  <files>static/style.css</files>
  <read_first>
    - static/style.css — read section 19 in full (all existing `::-webkit-scrollbar*` rules and the Firefox `scrollbar-color` rule); note current selectors so replacement is complete
    - static/style.css — confirm `.typeahead-dropdown` has `overflow-y: auto` and `.table-container` has `overflow-x: auto` (so scoped scrollbar rules will actually apply)
    - .planning/phases/16-visual-foundation/16-UI-SPEC.md — §Scrollbar Contract for exact values (4px, transparent, rgba white 0.2 / 0.4, border-radius 0)
    - .planning/phases/16-visual-foundation/16-CONTEXT.md — D-04, D-05, D-06
  </read_first>
  <action>
    In `static/style.css` section 19, REPLACE the entire existing scrollbar block with the new styling. The replacement must include three scopes:

    1. **Global page scrollbar** (WebKit — Chrome/Safari/Edge):
       ```css
       ::-webkit-scrollbar { width: 4px; height: 4px; }
       ::-webkit-scrollbar-track { background: transparent; }
       ::-webkit-scrollbar-thumb { background: rgba(255, 255, 255, 0.2); border-radius: 0; }
       ::-webkit-scrollbar-thumb:hover { background: rgba(255, 255, 255, 0.4); }
       ```

    2. **Scoped in-component scrollbars** — apply matching rules to `.typeahead-dropdown` and `.table-container` (D-05):
       ```css
       .typeahead-dropdown::-webkit-scrollbar,
       .table-container::-webkit-scrollbar { width: 4px; height: 4px; }
       .typeahead-dropdown::-webkit-scrollbar-track,
       .table-container::-webkit-scrollbar-track { background: transparent; }
       .typeahead-dropdown::-webkit-scrollbar-thumb,
       .table-container::-webkit-scrollbar-thumb { background: rgba(255, 255, 255, 0.2); border-radius: 0; }
       .typeahead-dropdown::-webkit-scrollbar-thumb:hover,
       .table-container::-webkit-scrollbar-thumb:hover { background: rgba(255, 255, 255, 0.4); }
       ```

    3. **Firefox** (D-06):
       ```css
       html {
         scrollbar-width: thin;
         scrollbar-color: rgba(255, 255, 255, 0.2) transparent;
       }
       ```

    Remove all prior scrollbar values: `#1a1a1a`, `#444444`, `#666666`, and any `width: 6px` / `width: 8px` scrollbar declarations. Do NOT leave legacy rules alongside new ones — this is a full replacement per D-04.
  </action>
  <verify>
    <automated>grep -cE "rgba\(255,\s*255,\s*255,\s*0\.2\)" static/style.css | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && ! grep -E "scrollbar.*#444444|scrollbar.*#666666|::-webkit-scrollbar\s*\{[^}]*width:\s*6px" static/style.css</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Global `::-webkit-scrollbar { width: 4px; ... }` rule present
    - [ ] Scoped `.typeahead-dropdown::-webkit-scrollbar` rules present
    - [ ] Scoped `.table-container::-webkit-scrollbar` rules present
    - [ ] Thumb at rest uses `rgba(255, 255, 255, 0.2)` (or equivalent spacing) in all three scopes
    - [ ] Thumb on `:hover` uses `rgba(255, 255, 255, 0.4)` in all three scopes
    - [ ] Track is `transparent` (not `#1a1a1a`, not `#000`, not `--color-surface-alt`)
    - [ ] Firefox `html { scrollbar-color: rgba(255, 255, 255, 0.2) transparent; scrollbar-width: thin; }` present
    - [ ] Previous scrollbar values (`#444444`, `#666666`, `width: 6px`, `width: 8px`) do NOT appear in any `::-webkit-scrollbar*` rule
    - [ ] Thumb `border-radius: 0` (matches `--radius-card: 0px` per UI-SPEC)
  </acceptance_criteria>
  <done>Section 19 fully replaced. Three scopes (global, typeahead, table) styled identically. Firefox matches. No legacy scrollbar color values remain in section 19.</done>
</task>

</tasks>

<verification>
  <automated>
    # 1. All four tokens at #000000
    grep -cE "^\s*--color-(bg|surface|surface-alt|cta-text):\s*#000000" static/style.css
    # Expected: 4

    # 2. Skeleton pulse new values present
    grep -cE "#0a0a0a|#141414" static/style.css
    # Expected: >= 2

    # 3. New scrollbar thumb values present (rgba white at 0.2 and 0.4)
    grep -cE "rgba\(255,\s*255,\s*255,\s*0\.2\)" static/style.css
    grep -cE "rgba\(255,\s*255,\s*255,\s*0\.4\)" static/style.css
    # Expected: each >= 2 (global + scoped)

    # 4. Scoped scrollbar selectors present
    grep -c "\.typeahead-dropdown::-webkit-scrollbar" static/style.css
    grep -c "\.table-container::-webkit-scrollbar" static/style.css
    # Expected: each >= 1

    # 5. Firefox rule
    grep -E "scrollbar-color:\s*rgba\(255,\s*255,\s*255,\s*0\.2\)\s*transparent" static/style.css
    # Expected: match

    # 6. Legacy values purged
    ! grep -E "scrollbar.*#444444|scrollbar.*#666666" static/style.css
  </automated>

  Manual sanity (not gating — Phase 16 Plan 02 owns human visual verification):
  - Open dashboard, item detail, and "Add to Wishlist" modal locally — surfaces should be `#000`
  - Scroll a long list — scrollbar should be 4px, translucent white, invisible track
  - Trigger the artwork skeleton — pulse should be visible but subtle
</verification>

<success_criteria>
- All three change areas in UI-SPEC §Implementation Checklist are complete
- No template, JS, font, spacing, or radius changes occurred (Phase 16 is CSS-only per CONTEXT.md §Phase Boundary)
- `static/style.css` still parses (site loads without CSS errors)
- VIS-01, VIS-02, VIS-03 requirements have their CSS foundation in place; Plan 02 gates final sign-off via human visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/16-visual-foundation/16-01-SUMMARY.md` documenting:
- Exact lines changed in each of the three change areas
- Whether `--color-border-muted` was raised from `#111111` (Claude discretion)
- Any skeleton pulse readability concern noted for the Plan 02 checkpoint
</output>
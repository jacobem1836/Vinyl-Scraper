---
phase: 04-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/style.css
  - templates/base.html
  - templates/index.html
  - templates/item_detail.html
autonomous: true
requirements:
  - POLISH-01
  - POLISH-02
  - POLISH-03
  - POLISH-04
  - POLISH-05

must_haves:
  truths:
    - "Dashboard uses near-black neutral background (#0a0a0a) with no blue cast"
    - "All accent elements are white, not amber/gold"
    - "Cards and containers have sharp edges (border-radius 0)"
    - "Nav bar displays CRATE as a wide-tracked wordmark"
    - "All page titles and copy reference CRATE, not Vinyl Wishlist"
    - "Card grid is denser — 12px gap and 12px card padding"
  artifacts:
    - path: "static/style.css"
      provides: "Updated design tokens, radius tokens, spinner, keyframe, nav-brand rule"
      contains: "--color-bg: #0a0a0a"
    - path: "templates/base.html"
      provides: "CRATE nav brand and page title"
      contains: "CRATE"
    - path: "templates/index.html"
      provides: "Dashboard title and empty state copy"
      contains: "Your crate is empty"
    - path: "templates/item_detail.html"
      provides: "Detail page title, delete copy, sharp artwork radius"
      contains: "Remove from Crate"
  key_links:
    - from: "static/style.css"
      to: "templates/*.html"
      via: "CSS custom properties consumed by class usage"
      pattern: "var\\(--color-"
---

<objective>
Update the CSS design system and all Jinja2 templates to implement the CRATE visual polish: near-black neutral palette, white accent replacing amber, sharp edges everywhere, compressed card spacing, and CRATE wordmark nav.

Purpose: Transform the app from a blue-tinted dark theme with amber accents into a sleek monochrome aesthetic (Linear/Vercel territory) and rebrand to CRATE.
Output: Updated style.css with new tokens and rules; updated base.html, index.html, item_detail.html with new copy and inline style fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-polish/04-CONTEXT.md
@.planning/phases/04-ui-polish/04-RESEARCH.md
@.planning/phases/04-ui-polish/04-UI-SPEC.md

<interfaces>
<!-- Current CSS tokens in static/style.css :root block (lines 10-75) -->
<!-- All colour, spacing, typography tokens are CSS custom properties -->
<!-- Phase 4 replaces colour values and adds --radius-* tokens -->

From static/style.css :root (current Phase 3 values to replace):
```css
--color-bg:           #0f172a;   /* -> #0a0a0a */
--color-surface:      #1e293b;   /* -> #111111 */
--color-surface-alt:  #0f172a;   /* -> #0a0a0a */
--color-border:       #334155;   /* -> #222222 */
--color-border-muted: #1e293b;   /* -> #111111 */
--color-text:         #f8fafc;   /* -> #f5f5f5 */
--color-text-muted:   #94a3b8;   /* -> #777777 */
--color-text-faint:   #64748b;   /* -> #555555 */
--color-accent:       #f59e0b;   /* -> #ffffff */
--color-accent-hover: #fbbf24;   /* -> #e0e0e0 */
--color-accent-bg:    #78350f;   /* -> rgba(255,255,255,0.08) */
--color-accent-cta:   #f59e0b;   /* -> #ffffff */
--color-cta-text:     #0f172a;   /* -> #0a0a0a */
```

From templates/base.html (line 6, 12):
```html
<title>{% block title %}Vinyl Wishlist{% endblock %}</title>
<a href="/" class="nav-brand">Vinyl Wishlist</a>
```

From templates/index.html (line 3, 171):
```html
{% block title %}Dashboard · Vinyl Wishlist{% endblock %}
<h3 ...>Your wishlist is empty</h3>
```

From templates/item_detail.html (line 3, 20, 26, 51):
```html
{% block title %}{{ item.query }} · Vinyl Wishlist{% endblock %}
style="... border-radius: 8px;"  (line 20 and 26)
onsubmit="return confirm('Remove this item from your wishlist?')"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSS design system — tokens, radius, spinner, keyframe, nav-brand, spacing</name>
  <files>static/style.css</files>
  <read_first>
    - static/style.css (full file — current token values, all border-radius instances, spinner rule, keyframe, nav-brand rule)
    - .planning/phases/04-ui-polish/04-UI-SPEC.md (authoritative token values, radius contract, badge tokens)
    - .planning/phases/04-ui-polish/04-RESEARCH.md (radius audit table, pitfall warnings)
  </read_first>
  <action>
Update `static/style.css` with the following exact changes. Reference the UI-SPEC as the source of truth for all values.

**1. Replace colour tokens in `:root` block (lines 10-57)** per D-03, D-04, D-05:

Background/surface tokens:
- `--color-bg: #0a0a0a;`
- `--color-surface: #111111;`
- `--color-surface-alt: #0a0a0a;`

Border tokens:
- `--color-border: #222222;`
- `--color-border-muted: #111111;`

Text tokens:
- `--color-text: #f5f5f5;`
- `--color-text-muted: #777777;`
- `--color-text-faint: #555555;`

Accent tokens (white replaces amber):
- `--color-accent: #ffffff;`
- `--color-accent-hover: #e0e0e0;`
- `--color-accent-bg: rgba(255,255,255,0.08);`
- `--color-accent-cta: #ffffff;`
- `--color-cta-text: #0a0a0a;`

Badge tokens — replace existing type badge and source badge tokens with desaturated variants per D-05:
- `--color-badge-album-bg: rgba(100, 120, 180, 0.15);`
- `--color-badge-album-text: #a0b0d0;`
- `--color-badge-album-border: rgba(100, 120, 180, 0.25);`
- `--color-badge-artist-bg: rgba(140, 100, 180, 0.15);`
- `--color-badge-artist-text: #b8a0d8;`
- `--color-badge-artist-border: rgba(140, 100, 180, 0.25);`
- `--color-badge-other-bg: rgba(80, 160, 100, 0.15);`
- `--color-badge-other-text: #90c8a0;`
- `--color-badge-other-border: rgba(80, 160, 100, 0.25);`
- `--color-source-discogs-bg: rgba(200, 200, 200, 0.1);`
- `--color-source-discogs-text: #bbbbbb;`
- `--color-source-other-bg: rgba(120, 140, 180, 0.1);`
- `--color-source-other-text: #9090b0;`

**2. Add radius tokens to `:root` block** per D-06:
```css
/* Border radius */
--radius-card:    0px;
--radius-modal:   0px;
--radius-btn:     2px;
--radius-badge:   2px;
--radius-input:   2px;
```

**3. Replace ALL hardcoded `border-radius` values** per the radius audit (D-06):
- `.btn-cta` (line ~184): `border-radius: 6px` -> `border-radius: var(--radius-btn);`
- `.btn-secondary` (line ~203): `border-radius: 6px` -> `border-radius: var(--radius-btn);`
- `.btn-destructive` (line ~221): `border-radius: 6px` -> `border-radius: var(--radius-btn);`
- `.card` (line ~238): `border-radius: 8px` -> `border-radius: var(--radius-card);`
- `.card-artwork` (line ~270): `border-radius: 4px 4px 0 0` -> `border-radius: 0;`
- `.form-input` (line ~289): `border-radius: 6px` -> `border-radius: var(--radius-input);`
- `.modal-panel` (line ~340): `border-radius: 12px` -> `border-radius: var(--radius-modal);`
- `.scan-card` (line ~351): `border-radius: 12px` -> `border-radius: 0;`
- `.badge` (line ~385): `border-radius: 9999px` -> `border-radius: var(--radius-badge);`
- `.scan-pill` (line ~457): `border-radius: 9999px` -> `border-radius: 2px;`
- `.empty-state` (line ~472): `border-radius: 12px` -> `border-radius: 0;`
- `.spinner` (line ~487): **KEEP** `border-radius: 50%` — this is functional (circular shape), NOT decorative
- `.toast` (line ~504): `border-radius: 8px` -> `border-radius: 0;`
- `.table-container` (line ~526): `border-radius: 12px` -> `border-radius: 0;`

**4. Update `.spinner` border colours** (replace hardcoded amber):
```css
border: 2px solid rgba(255, 255, 255, 0.2);
border-top-color: var(--color-accent);
```
Replace existing `border: 2px solid rgba(245, 158, 11, 0.3);` and `border-top-color: #f59e0b;`.

**5. Update `@keyframes pulse-border`** (replace amber rgba values):
```css
@keyframes pulse-border {
  0%, 100% { border-color: rgba(255, 255, 255, 0.2); }
  50%       { border-color: rgba(255, 255, 255, 0.6); }
}
```

**6. Update `.nav-brand` rule** per D-08:
```css
.nav-brand {
  font-size: var(--text-body);
  font-weight: 600;
  letter-spacing: 0.25em;
  text-transform: uppercase;
  line-height: 1;
  color: var(--color-accent);
}
```
Remove any existing font-size, font-weight, or color properties in the current `.nav-brand` rule and replace with the above.

**7. Compress card/grid spacing** per D-07 — update component rules (NOT global tokens):
- `.card-body` padding: change from `var(--space-md)` (or `16px`) to `12px`
- `.card-grid` gap: change from `var(--space-lg)` (or `24px`) to `12px`
- Find any `.section-gap` or equivalent section spacing rule and change to `20px` if it exists

**8. Update CSS file header comment:** Change "Vinyl Wishlist" to "CRATE" per D-01.

CRITICAL: Do NOT change the global `--space-*` token values. Only the three component-level overrides above are compressed.
CRITICAL: Do NOT remove `border-radius: 50%` from `.spinner`.
  </action>
  <verify>
    <automated>grep -c "#0f172a\|#1e293b\|#334155\|#f59e0b\|#fbbf24\|#78350f\|#94a3b8\|#64748b\|#f8fafc" static/style.css | grep "^0$" && echo "PASS: No Phase 3 colour values remain" || echo "FAIL: Phase 3 colour values still present"</automated>
  </verify>
  <acceptance_criteria>
    - static/style.css contains `--color-bg: #0a0a0a`
    - static/style.css contains `--color-accent: #ffffff`
    - static/style.css contains `--color-cta-text: #0a0a0a`
    - static/style.css contains `--radius-card: 0px`
    - static/style.css contains `--radius-btn: 2px`
    - static/style.css contains `--color-badge-album-text: #a0b0d0`
    - static/style.css contains `--color-source-discogs-text: #bbbbbb`
    - static/style.css does NOT contain `#f59e0b` (amber removed)
    - static/style.css does NOT contain `#0f172a` (blue-tinted bg removed)
    - static/style.css contains `border-radius: 50%` (spinner preserved)
    - static/style.css contains `letter-spacing: 0.25em` in `.nav-brand`
    - static/style.css contains `rgba(255, 255, 255, 0.2)` (updated spinner/keyframe)
    - `.card-body` rule contains `padding: 12px` (not 16px)
    - `.card-grid` rule contains `gap: 12px`
    - `--space-md` remains `16px` (global tokens unchanged)
  </acceptance_criteria>
  <done>All Phase 3 colour values replaced with Phase 4 neutral/white palette. Radius tokens added and all hardcoded border-radius values updated per the radius contract. Spinner, keyframe, and nav-brand rule updated. Card spacing compressed. No amber/gold (#f59e0b) or blue-tinted (#0f172a) values remain in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Update template text — CRATE rename, copy changes, inline radius fixes</name>
  <files>templates/base.html, templates/index.html, templates/item_detail.html</files>
  <read_first>
    - templates/base.html (current nav brand text, title tag)
    - templates/index.html (current title block, empty state heading at line 171)
    - templates/item_detail.html (current title block, inline border-radius on lines 20 and 26, delete form at line 51)
    - .planning/phases/04-ui-polish/04-UI-SPEC.md (Copywriting Contract section — authoritative copy values)
  </read_first>
  <action>
Update all three template files with the CRATE rename and copy changes per D-01, D-02, and the UI-SPEC Copywriting Contract.

**templates/base.html:**
1. Line 6 — change `<title>{% block title %}Vinyl Wishlist{% endblock %}</title>` to `<title>{% block title %}CRATE{% endblock %}</title>`
2. Line 12 — change `<a href="/" class="nav-brand">Vinyl Wishlist</a>` to `<a href="/" class="nav-brand">CRATE</a>`
   Note: The CSS `text-transform: uppercase` and `letter-spacing: 0.25em` in `.nav-brand` (set in Task 1) handle the wordmark display. HTML text is just `CRATE`.

**templates/index.html:**
1. Line 3 — change `{% block title %}Dashboard · Vinyl Wishlist{% endblock %}` to `{% block title %}Dashboard · CRATE{% endblock %}`
2. Line 171 — change the empty state heading text from `Your wishlist is empty` to `Your crate is empty`

**templates/item_detail.html:**
1. Line 3 — change `{% block title %}{{ item.query }} · Vinyl Wishlist{% endblock %}` to `{% block title %}CRATE — {{ item.query }}{% endblock %}`
   Note: Use em dash (—), not middle dot, per UI-SPEC. Format is "CRATE — {item.query}".
2. Line 20 — change `border-radius: 8px` to `border-radius: 0` in the inline style on the artwork `<img>` element
3. Line 26 — change `border-radius: 8px` to `border-radius: 0` in the inline style on the placeholder artwork `<img>` element
4. Line 51 — change the delete form:
   - `onsubmit="return confirm('Remove this item from your wishlist?')"` -> `onsubmit="return confirm('Remove this item from your crate?')"`
   - Delete button text: change `Delete` to `Remove from Crate`

Do NOT change any other HTML structure, classes, or JavaScript. Text-only and inline-style-only edits.
  </action>
  <verify>
    <automated>grep -r "Vinyl Wishlist" templates/ && echo "FAIL: Old name still present" || echo "PASS: No Vinyl Wishlist references in templates" ; grep "Your crate is empty" templates/index.html && echo "PASS: Empty state updated" || echo "FAIL: Empty state not updated" ; grep "Remove from Crate" templates/item_detail.html && echo "PASS: Delete button updated" || echo "FAIL: Delete button not updated" ; grep -c "border-radius: 8px" templates/item_detail.html | grep "^0$" && echo "PASS: Inline radius removed" || echo "FAIL: Inline border-radius: 8px still present"</automated>
  </verify>
  <acceptance_criteria>
    - No file in templates/ contains the string "Vinyl Wishlist"
    - templates/base.html contains `>CRATE</a>` in the nav-brand link
    - templates/base.html contains `CRATE{% endblock %}` in the title block
    - templates/index.html contains `Dashboard · CRATE`
    - templates/index.html contains `Your crate is empty`
    - templates/item_detail.html contains `CRATE — {{ item.query }}`
    - templates/item_detail.html contains `Remove from Crate`
    - templates/item_detail.html contains `Remove this item from your crate?`
    - templates/item_detail.html does NOT contain `border-radius: 8px`
    - templates/item_detail.html contains `border-radius: 0` on both artwork img elements
  </acceptance_criteria>
  <done>All templates reference CRATE instead of Vinyl Wishlist. Empty state says "Your crate is empty". Delete button says "Remove from Crate" with updated confirm copy. Inline border-radius: 8px removed from both artwork images in item_detail.html (set to 0).</done>
</task>

</tasks>

<verification>
1. `grep -r "Vinyl Wishlist" static/ templates/` returns no matches
2. `grep -c "#f59e0b\|#0f172a\|#1e293b\|#334155" static/style.css` returns 0
3. `grep "border-radius: 50%" static/style.css` returns a match (spinner preserved)
4. `grep "letter-spacing: 0.25em" static/style.css` returns a match (wordmark)
5. `grep "Your crate is empty" templates/index.html` returns a match
6. `grep "Remove from Crate" templates/item_detail.html` returns a match
7. `python -c "from app.main import app; print('OK')"` exits 0 (app still starts)
</verification>

<success_criteria>
- Near-black neutral palette (#0a0a0a bg) with white accent (#ffffff) — no amber or blue-tinted values remain
- All border-radius values follow the radius contract (0 for cards/containers, 2px for buttons/badges/inputs, 50% for spinner only)
- CRATE wordmark displays in nav with letter-spacing 0.25em
- All page titles, copy, and headings reference CRATE — zero instances of "Vinyl Wishlist" in templates
- Card grid gap and card padding compressed to 12px
- iOS Shortcut API contract unchanged (no router/API changes in this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-polish/04-01-SUMMARY.md`
</output>

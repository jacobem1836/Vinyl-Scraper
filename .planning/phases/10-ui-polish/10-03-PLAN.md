---
phase: 10-ui-polish
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified: []
autonomous: false
requirements:
  - UIP-01
  - UIP-02
  - UIP-03
  - UIP-04
  - UIP-05
  - UIP-06
  - UIP-07
  - UIP-08
  - UIP-09
  - UIP-10
  - BUG-01

must_haves:
  truths:
    - "All 11 requirements verified as implemented or preserved"
    - "Design tool audit confirms hierarchy improvements"
    - "No visual regressions from Phase 5 carry-forwards"
  artifacts: []
  key_links: []
---

<objective>
Run the design-for-ai checker audit on the updated UI and visually verify all 11 requirements are satisfied. This is the final quality gate before the phase is marked complete.

Purpose: Confirm the design system update achieves the intended visual hierarchy improvements and all formal requirements pass.
Output: Verification results confirming all requirements met.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-polish/10-CONTEXT.md
@.planning/phases/10-ui-polish/10-UI-SPEC.md
@.planning/phases/10-ui-polish/10-01-SUMMARY.md
@.planning/phases/10-ui-polish/10-02-SUMMARY.md

@static/style.css
@templates/index.html
@templates/item_detail.html
@templates/base.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification of all 11 requirements</name>
  <files></files>
  <read_first>
    - static/style.css (verify tokens and rules)
    - templates/index.html (verify card tiers, stats bar, card titles)
    - templates/item_detail.html (verify H2 tokens, delete copy, scan button)
    - templates/base.html (verify disabled button JS)
    - static/typeahead.js (verify no-results copy and spinner fix referenced)
  </read_first>
  <action>
Run automated checks against each requirement. For each, grep or read the relevant file to confirm the implementation:

**UIP-01:** `grep "color-text-faint.*#686868" static/style.css` — must match
**UIP-02:** `grep "text-body" templates/index.html` — card title h3 must reference text-body (16px), NOT text-sm (14px)
**UIP-03:** `grep "text-subheading" templates/item_detail.html` — "Best Deals" H2 uses subheading token; `grep "text-body.*All Listings\|All Listings" templates/item_detail.html` — "All Listings" uses body size
**UIP-04:** `grep "focus-visible" static/style.css` — btn-cta, btn-secondary, btn-destructive all have :focus-visible
**UIP-05:** `grep "scale(0.97)" static/style.css` — :active rules present
**UIP-06:** `grep "btn.*disabled" static/style.css` — disabled CSS exists; `grep "btn.disabled" templates/base.html` — JS disables buttons
**UIP-07:** `grep 'role="dialog"' templates/index.html templates/item_detail.html` — both modals have role=dialog; `grep 'aria-modal="true"' templates/index.html templates/item_detail.html` — both have aria-modal
**UIP-08:** `grep "Confirm Delete" templates/item_detail.html` — confirm button copy; `grep "confirm(" templates/item_detail.html` — must NOT match (no native confirm())
**UIP-09:** `grep "repeat(3, 1fr)" static/style.css` — 3-column breakpoint at 1024px
**UIP-10:** Search for bare `12px` in component rules (not :root tokens) in static/style.css — should find none
**BUG-01:** `grep "scan-panel" templates/base.html | grep "position.*fixed"` or confirm scan panel is fixed-positioned and doesn't overlap with page content buttons

Report results as a checklist with PASS/FAIL for each.

Then run `design-for-ai` CHECKER mode on all changed files to generate the final design audit:
- Run checker on templates/index.html
- Run checker on templates/item_detail.html
- Confirm the three original FLAGs (Typography, Composition, Visual Hierarchy) from the 10-DESIGN-AUDIT.md are resolved

If any requirement fails, fix it immediately (the fix should be small since Plans 01-02 did the heavy lifting).
  </action>
  <verify>
    <automated>grep -q "#686868" static/style.css && grep -q "card--deal" templates/index.html && grep -q "text-subheading" templates/item_detail.html && grep -q "focus-visible" static/style.css && grep -q "scale(0.97)" static/style.css && grep -q "disabled" static/style.css && grep -q 'role="dialog"' templates/index.html && grep -q "Confirm Delete" templates/item_detail.html && ! grep -q "confirm(" templates/item_detail.html && grep -q "repeat(3, 1fr)" static/style.css && echo "ALL 11 REQUIREMENTS PASS" || echo "SOME REQUIREMENTS FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - UIP-01: static/style.css contains `--color-text-faint: #686868` — PASS
    - UIP-02: templates/index.html card h3 uses `var(--text-body)` not text-sm — PASS
    - UIP-03: item_detail.html Best Deals H2 uses text-subheading class — PASS
    - UIP-04: static/style.css has :focus-visible on all button classes — PASS
    - UIP-05: static/style.css has :active scale(0.97) — PASS
    - UIP-06: static/style.css has :disabled rules; base.html JS disables buttons — PASS
    - UIP-07: Both modals have role="dialog" and aria-modal="true" — PASS
    - UIP-08: item_detail.html uses "Confirm Delete"/"Keep Record" and has zero `confirm()` calls — PASS
    - UIP-09: static/style.css has 3-column grid at 1024px — PASS
    - UIP-10: No bare 12px values in CSS component rules — PASS
    - BUG-01: Scan panel is fixed-positioned, no overlap with page buttons — PASS
    - design-for-ai CHECKER confirms typography, composition, and visual hierarchy FLAGs resolved
  </acceptance_criteria>
  <done>All 11 phase requirements verified as passing. Design tool audit confirms hierarchy improvements.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete UI polish</name>
  <files></files>
  <action>
Present the verification checklist to the user for manual visual inspection of the updated UI. Start the dev server if not running and guide through each check.
  </action>
  <verify>User confirms approval or reports issues</verify>
  <done>User approves the visual result or issues are identified and fixed.</done>
  <what-built>Complete UI polish pass: widened type scale (4 sizes, 2 weights), 3-tier spacing hierarchy, deal/no-deal card visual tiers with badges, upgraded stats bar, H1/H2 distinction via tokens, disabled scan buttons during scan, updated delete confirmation copy, table border cleanup, typeahead spinner fix, and all Phase 5 accessibility carry-forwards preserved.</what-built>
  <how-to-verify>
    1. Start the dev server: `python -m uvicorn app.main:app --reload --port 8000`
    2. Open http://localhost:8000 in a browser

    **Dashboard checks:**
    - Stats bar: values should be large (22px) and labels small (12px) and muted
    - Card titles: should be 16px body size, weight 500
    - Cards with deals (price below notify threshold): should have green left border and "DEAL -X%" badge
    - Cards with no listings: should be dimmed (50% opacity) with dashed border
    - Card grid: resize browser to 1024px width — should show 3 columns
    - Click "Scan All": button should become disabled (dimmed, not clickable) during scan

    **Detail page checks:**
    - Click a card to go to detail page
    - H1 (item name): large (36px)
    - H2 "Best Deals": medium (22px, bold)
    - H2 "All Listings": smaller (16px, normal weight)
    - Table: no row borders, numeric columns right-aligned
    - "Scan Now" button: click it, should become disabled during scan
    - "Remove from Crate" should show "Confirm Delete" / "Keep Record" (not browser confirm dialog)

    **Keyboard accessibility:**
    - Tab to any button: should show white focus ring
    - Press Enter on a focused button: should show 0.97 scale press animation

    **Typeahead check:**
    - Click "Add Item" in nav
    - Type an album name: spinner should appear while loading
    - Select a result: spinner should disappear immediately (not linger)

    3. Report any visual issues or regressions
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan — no code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | N/A | N/A | N/A | Verification plan — no new attack surface |
</threat_model>

<verification>
All 11 requirements verified via automated grep checks and manual visual inspection.
</verification>

<success_criteria>
All 11 requirements (UIP-01 through UIP-10 and BUG-01) pass automated and visual verification. User approves the visual result at the checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-polish/10-03-SUMMARY.md`
</output>

---
phase: 10-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/style.css
autonomous: true
requirements:
  - UIP-01
  - UIP-02
  - UIP-04
  - UIP-05
  - UIP-10
  - UIP-06
  - UIP-09

must_haves:
  truths:
    - "Type scale has exactly 4 declared sizes (12, 16, 22, 36px) and 2 weights (400, 700)"
    - "Spacing tiers are applied: card-grid gap is 16px, stack-lg gap is 48px"
    - "Table rows have no border-top and numeric columns are right-aligned"
    - "Disabled buttons show opacity 0.5 and cursor not-allowed"
    - "Typeahead spinner stops animation when hidden"
    - "Card tier CSS classes exist for deal and empty states"
  artifacts:
    - path: "static/style.css"
      provides: "All design token changes and component rule updates"
      contains: "--text-label"
  key_links:
    - from: "static/style.css :root"
      to: "static/style.css component rules"
      via: "var() references"
      pattern: "var\\(--text-(label|body|subheading|heading)\\)"
---

<objective>
Overhaul the CSS design system tokens and component rules in style.css to implement the widened type scale (D-07), 3-tier spacing hierarchy (D-08), table border removal (D-11), card title weight reduction (D-12), disabled button states (UIP-06/D-05), typeahead spinner fix, and card tier CSS classes (D-09).

Purpose: All downstream template changes in Plan 02 depend on these tokens and classes existing in CSS first.
Output: Updated static/style.css with new tokens, modified component rules, and new card tier classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-polish/10-CONTEXT.md
@.planning/phases/10-ui-polish/10-RESEARCH.md
@.planning/phases/10-ui-polish/10-UI-SPEC.md

@static/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update design tokens and typography helpers in :root and utility classes</name>
  <files>static/style.css</files>
  <read_first>
    - static/style.css (current token values at lines 18-91, typography helpers at lines 467-479)
    - .planning/phases/10-ui-polish/10-UI-SPEC.md (token mapping table)
    - .planning/phases/10-ui-polish/10-CONTEXT.md (D-07, D-08, D-12 decisions)
  </read_first>
  <action>
IMPORTANT: Before making any design decisions, invoke the required design tools per D-01:
1. Run `python3 ~/.claude/skills/ui-ux-pro-max/scripts/search.py "vinyl brutalist dark utility personal tool dashboard" --design-system -p "CRATE"` for design system reference
2. Use `mcp__stitch__*` to preview token changes before writing code
3. Use `mcp__magic__*` for card component pattern reference

Then modify static/style.css as follows:

**A) Replace typography tokens in :root block (around lines 83-88):**

Remove these tokens:
```
--text-sm:      14px;
--text-price:   20px;
```

Add/change to exactly these 4 tokens:
```
--text-label:      12px;
--text-body:       16px;
--text-subheading: 22px;
--text-heading:    36px;
```

Note: `--text-body: 16px` already exists unchanged. `--text-heading` changes from 28px to 36px.

Add a new font token for monospace (D-13 discretion -- include if design tool audit confirms, skip if not):
```
--font-mono: "JetBrains Mono", monospace;
```
If including JetBrains Mono, self-host the woff2 file in static/fonts/ with an @font-face rule matching the BodoniModa pattern.

**B) Update spacing application (D-08):**

In `.card-grid` rule (around line 287): change `gap: var(--space-sm)` to `gap: var(--space-md)` (inter-card = 16px).

In `.stack-lg` rule (around line 159): change `gap: var(--space-lg)` to `gap: var(--space-2xl)` (inter-section = 48px).

**C) Update typography helper classes (around lines 467-470):**

Replace the existing typography helper block with:
```css
.text-label   { font-size: var(--text-label); }
.text-body    { font-size: var(--text-body); }
.text-subheading { font-size: var(--text-subheading); font-weight: 700; line-height: 1.2; }
.text-heading { font-size: var(--text-heading); font-weight: 700; line-height: 1.1; }
```

Remove `.text-sm` and `.text-price` helper classes. Keep `.text-muted`, `.text-faint`, `.text-accent`, `.text-destructive`, `.text-success`, `.price-best` unchanged.

**D) Update all component rules that reference removed tokens:**

Replace every `var(--text-sm)` with `var(--text-label)` or `var(--text-body)` as appropriate:
- `.btn-cta`, `.btn-secondary`, `.btn-destructive` font-size: change `var(--text-sm)` to `var(--text-body)` (buttons should be body size)
- `.form-label` font-size: keep at a small size, change `var(--text-sm)` to `var(--text-label)` (12px for form labels)
- `.table` font-size: change `var(--text-sm)` to `var(--text-body)` (table cells at body size 16px)
- `.badge` font-size: change `var(--text-sm)` to `var(--text-label)` (badges at 12px)
- `.typeahead-empty` font-size: change `var(--text-sm)` to `var(--text-label)`
- `.typeahead-pin-badge` font-size: change `var(--text-sm)` to `var(--text-label)`

Replace `var(--text-price)` references: `.text-price` helper class is removed. Any rule using `var(--text-price)` should use `var(--text-heading)` (36px) instead per UI-SPEC.

**E) Update card title weight (D-12):**
No dedicated `.card-title` class exists yet. The card titles in templates use `class="text-sm"` with inline `font-weight: 600`. The template changes (Plan 02) will handle the class change. No CSS-side action needed here beyond ensuring the token exists.

**F) Verify UIP-01 (--color-text-faint):**
Confirm `--color-text-faint: #686868` is still present in :root. Do not change it.

**G) Verify UIP-04/UIP-05 (focus-visible/active):**
Confirm the `:focus-visible` and `:active` rules for btn-cta, btn-secondary, btn-destructive are still present at lines 254-265. Do not change them.

**H) Verify UIP-09 (3-column grid):**
Confirm the `@media (min-width: 1024px) { .card-grid { grid-template-columns: repeat(3, 1fr); } }` rule exists at lines 295-299. Do not change it.
  </action>
  <verify>
    <automated>grep -c "text-label" static/style.css | grep -q "[1-9]" && grep -c "text-subheading" static/style.css | grep -q "[1-9]" && grep "text-heading" static/style.css | grep -q "36px" && grep "card-grid" static/style.css | grep -q "space-md" && grep "stack-lg" static/style.css | grep -q "space-2xl" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - static/style.css :root contains `--text-label: 12px`
    - static/style.css :root contains `--text-subheading: 22px`
    - static/style.css :root contains `--text-heading: 36px`
    - static/style.css :root does NOT contain `--text-sm:` or `--text-price:`
    - static/style.css `.card-grid` rule contains `gap: var(--space-md)`
    - static/style.css `.stack-lg` rule contains `gap: var(--space-2xl)`
    - static/style.css contains `.text-label` helper class
    - static/style.css contains `.text-subheading` helper class with `font-weight: 700`
    - static/style.css does NOT contain `.text-sm` helper class (the class selector itself)
    - static/style.css contains `--color-text-faint: #686868` (UIP-01 verified)
    - static/style.css contains `:focus-visible` rules for buttons (UIP-04 verified)
    - static/style.css contains `:active` rules with `scale(0.97)` (UIP-05 verified)
    - static/style.css contains `repeat(3, 1fr)` at 1024px media query (UIP-09 verified)
    - No references to `var(--text-sm)` remain in style.css
    - No references to `var(--text-price)` remain in style.css
  </acceptance_criteria>
  <done>All 4 typography tokens defined (12, 16, 22, 36px), removed tokens purged, spacing tiers applied, all component rules updated to use new tokens, Phase 5 carry-forwards verified present.</done>
</task>

<task type="auto">
  <name>Task 2: Add card tier CSS, disabled button state, table cleanup, and spinner fix</name>
  <files>static/style.css</files>
  <read_first>
    - static/style.css (current card rules at lines 267-321, button rules at lines 198-265, table rules at lines 388-418, typeahead spinner at lines 686-698)
    - .planning/phases/10-ui-polish/10-CONTEXT.md (D-09 card tiers, D-05 disabled, D-11 table)
    - .planning/phases/10-ui-polish/10-UI-SPEC.md (interaction contract, card tier colors)
  </read_first>
  <action>
**A) Add card tier CSS classes after the existing `.card--scanning` rule (around line 321):**

```css
/* Card tier — deal (D-09 Tier 1) */
.card--deal {
  border-left: 3px solid var(--color-success);
}

/* Card tier — empty/no listings (D-09 Tier 3) */
.card--empty {
  opacity: 0.5;
  border-style: dashed;
}

/* Deal badge on card */
.card-deal-badge {
  position: absolute;
  top: var(--space-sm);
  right: var(--space-sm);
  background: var(--color-success);
  color: var(--color-bg);
  font-size: var(--text-label);
  font-weight: 700;
  padding: 2px var(--space-sm);
  border-radius: var(--radius-badge);
  letter-spacing: 0.05em;
  text-transform: uppercase;
  z-index: 1;
}
```

Also add `position: relative` to the existing `.card` rule so the deal badge can be absolutely positioned within it.

**B) Add disabled button state (UIP-06, D-05):**

After the existing `:active` rules (around line 265), add:

```css
.btn-cta:disabled,
.btn-secondary:disabled,
.btn-destructive:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  pointer-events: none;
}
```

**C) Update table rules (D-11):**

In `.table td` rule (around line 411-414):
- Remove `border-top: 1px solid rgba(51, 65, 85, 0.7);`
- Change padding to `10px var(--space-md)` (increased vertical padding to compensate for removed border)

Add right-alignment for numeric columns:
```css
.table .col-price,
.table .col-landed {
  text-align: right;
}
```

**D) Fix typeahead spinner animation when hidden:**

Add this rule after the existing `.typeahead-spinner` rule (around line 698):

```css
.typeahead-spinner.hidden {
  animation: none;
}
```

**E) Replace any remaining rogue 12px hardcoded values (UIP-10):**

Search style.css for any bare `12px` values NOT inside a token definition. If found in component rules (not in the :root `--text-label: 12px` definition), replace with `var(--text-label)` or the closest spacing token.

After implementing, run `design-for-ai` CHECKER mode on static/style.css to confirm hierarchy improvements hold.
  </action>
  <verify>
    <automated>grep -c "card--deal" static/style.css | grep -q "[1-9]" && grep -c "card--empty" static/style.css | grep -q "[1-9]" && grep -c "btn-cta:disabled" static/style.css | grep -q "[1-9]" && grep -c "typeahead-spinner.hidden" static/style.css | grep -q "[1-9]" && grep "\.table td" static/style.css | grep -qv "border-top" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - static/style.css contains `.card--deal` with `border-left: 3px solid var(--color-success)`
    - static/style.css contains `.card--empty` with `opacity: 0.5`
    - static/style.css contains `.card-deal-badge` class
    - static/style.css contains `.card` rule with `position: relative`
    - static/style.css contains `.btn-cta:disabled` with `opacity: 0.5` and `cursor: not-allowed`
    - static/style.css `.table td` does NOT contain `border-top`
    - static/style.css `.table td` padding is `10px var(--space-md)`
    - static/style.css contains `.table .col-price` with `text-align: right`
    - static/style.css contains `.typeahead-spinner.hidden` with `animation: none`
    - No bare `12px` values in component rules (only in :root token definition)
  </acceptance_criteria>
  <done>Card tier CSS classes, disabled button state, table border cleanup, numeric column alignment, and typeahead spinner fix are all in style.css. UIP-10 rogue 12px values replaced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS only | No trust boundary crossed — pure presentation changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | D (Denial of Service) | CSS animation | accept | Spinner animation stopped via `.hidden { animation: none }` — no persistent CPU drain |
</threat_model>

<verification>
1. All 4 typography tokens exist in :root (12, 16, 22, 36px)
2. No references to removed tokens (--text-sm, --text-price) remain
3. Card tier classes exist (.card--deal, .card--empty, .card-deal-badge)
4. Disabled button CSS exists
5. Table td has no border-top
6. Typeahead spinner hidden state stops animation
7. Phase 5 carry-forwards still present (UIP-01, UIP-04, UIP-05, UIP-09)
</verification>

<success_criteria>
static/style.css contains the complete design system update: 4 typography tokens, 3-tier spacing, card tier classes, disabled button state, cleaned table rules, and spinner fix. Zero references to removed tokens. All Phase 5 implementations preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-polish/10-01-SUMMARY.md`
</output>

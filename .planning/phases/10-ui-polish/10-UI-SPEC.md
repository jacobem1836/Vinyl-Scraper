---
phase: 10
slug: ui-polish
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-08
---

# Phase 10 — UI Design Contract

> Visual and interaction contract for the UI Polish phase. Generated by gsd-ui-researcher.
> Consumed by gsd-ui-checker, gsd-planner, gsd-executor, and gsd-ui-auditor.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none — hand-rolled CSS custom properties | CONTEXT.md (code_context) |
| Preset | not applicable | no shadcn, no Tailwind |
| Component library | none | project uses Jinja2 templates + vanilla JS |
| Icon library | none | no icon library in project |
| Font | Bodoni Moda (display/brand only, self-hosted woff2); system sans-serif stack for all body/UI | static/style.css @font-face |

**shadcn gate:** Not executed — project is Python/FastAPI with Jinja2 templates, not React/Next.js/Vite. shadcn is not applicable.

---

## Spacing Scale

All tokens already defined in `static/style.css :root`. No changes to token names or values. Usage assignments are being corrected as part of this phase.

| Token | Value | Current Usage | Phase 10 Usage |
|-------|-------|---------------|----------------|
| `--space-xs` | 4px | intra-card element gaps | intra-card element gaps (unchanged) |
| `--space-sm` | 8px | compact element spacing | intra-card padding (correct tier) |
| `--space-md` | 16px | default element spacing | inter-card gap in `.card-grid` (upgraded from 8px) |
| `--space-lg` | 24px | section padding | section padding within zones |
| `--space-xl` | 32px | layout gaps | layout gaps |
| `--space-2xl` | 48px | major section breaks | inter-section gap (`.stack-lg` target) |
| `--space-3xl` | 64px | page-level spacing | page-level spacing (unchanged) |

**3-tier spacing correction (D-08):**

| Tier | Target Value | Token | Where Applied |
|------|-------------|-------|---------------|
| Intra-card | 6–8px | `var(--space-xs)` to `var(--space-sm)` | `.card-body` gap |
| Inter-card | 16px | `var(--space-md)` | `.card-grid` gap |
| Inter-section | 40–48px | `var(--space-2xl)` | `.stack-lg` gap between dashboard zones |

Source: D-08 (CONTEXT.md), RESEARCH.md Architecture Patterns / Spacing Tier Application.

Exceptions:
- Table row padding: `var(--space-md)` (16px) vertical padding on `.table td` — standard token, no off-scale overrides (source: D-11)
- Stats bar label/value spacing: 2–4px between value and label within a stat unit (use `var(--space-xs)`)

---

## Typography

### Token Changes (D-07)

The existing scale is compressed. This phase widens it and reduces it to exactly 4 sizes and 2 weights.

**Declared font sizes (4 total):**

| Token | Size | Weight | Line Height | Where Used |
|-------|------|--------|-------------|-----------|
| `--text-label` | 12px | 400 | 1.4 | Stats bar labels, badge text, muted metadata |
| `--text-body` | 16px | 400 | 1.5 | Card titles, card descriptions, table cells, form inputs, modal body, nav items, helper text, "All Listings" H2 |
| `--text-subheading` | 22px | 700 | 1.2 | "Best Deals" H2, stats bar values, section headings |
| `--text-heading` | 36px | 700 | 1.1 | Page-level H1s, card best price (deal cards), detail page deal price |

**Declared font weights (2 total):** 400 (default) and 700 (emphasized).

**Token mapping changes:**

| Token | Old Value | New Value | Notes |
|-------|-----------|-----------|-------|
| `--text-label` | (new) | 12px | New token |
| `--text-sm` | 14px | removed | Elements previously at 14px reassigned to `--text-body` (16px) |
| `--text-body` | 16px | 16px | Unchanged |
| `--text-title` | (new) | removed | Card album names use `--text-body` (16px); size hierarchy carried by weight only at card level |
| `--text-heading-secondary` | (new) | removed | "All Listings" H2 uses `--text-body` (16px) at weight 400 — visually subordinate to 22px "Best Deals" |
| `--text-price` | 20px | removed | Price signal uses `--text-heading` (36px) |
| `--text-subheading` | (new) | 22px | D-04 — H2 section headings, stats bar values |
| `--text-heading` | 28px | 36px | D-07 — page-level H1s and price signal |

**Two-family limit (enforced):** System sans-serif (`--font-sans`) for all UI text. Bodoni Moda (`--font-display`) restricted to CRATE brand wordmark only — Bodoni hairlines vanish below 30px.

**D-13 (monospace font — discretion):** Include JetBrains Mono for price cells. Rationale: at 36px price size, monospace reinforces "data vs label" distinction within the two-family limit (brand display font + mono = two families; system sans-serif is the default, not a named family). Self-host woff2 in `static/fonts/` consistent with BodoniModa pattern. Apply via a new `--font-mono` token. If the design tool audit (magic MCP, ui-ux-pro-max) during execution confirms size alone is sufficient, skip the font addition.

Source: D-07, D-04, D-10, D-12, D-13 (CONTEXT.md); RESEARCH.md Standard Stack / Code Examples.

---

## Color

All color tokens are already defined. No new palette tokens required for this phase. Usage assignments are clarified below.

| Role | Value | Token | Usage |
|------|-------|-------|-------|
| Dominant (60%) | `#0a0a0a` | `--color-bg` | Page background, surface-alt |
| Secondary (30%) | `#111111` | `--color-surface` | Cards, modal backgrounds, nav |
| Accent (10%) | `#ffffff` | `--color-accent` | See accent reserved list below |
| Destructive | `#f87171` | `--color-destructive` | Delete button, destructive confirmation copy |
| Success / deal signal | `#34d399` | `--color-success` | Deal card left-border accent (Tier 1 cards only) |
| Muted text | `#686868` | `--color-text-faint` | Stats bar labels, secondary metadata (UIP-01 verified at WCAG AA) |

**Accent (`#ffffff`) reserved for:**
- CTA button background (`.btn-cta`)
- Focus-visible ring outline (`box-shadow: 0 0 0 2px var(--color-accent)`)
- Card hover aura (1px white box-shadow)
- CRATE wordmark

**Accent NOT used for:** general body text, card borders, table headers, stats values, badge text.

**Card tier colors (D-09):**

| Tier | Condition | Visual Treatment |
|------|-----------|-----------------|
| Tier 1 — Deal | `has_deal = True` | `border-left: 3px solid var(--color-success)` |
| Tier 2 — Normal | has listings, no exceptional deal | Standard card — no modification |
| Tier 3 — Empty / scanning | no listings or awaiting first scan | `opacity: 0.5`, `border-style: dashed` |

Source: D-09, D-05 (CONTEXT.md); RESEARCH.md Code Examples.

Dark-only. No light mode — out of scope (REQUIREMENTS.md Out of Scope).

---

## Copywriting Contract

This phase adds no new CTA flows. Copy requirements are confined to state labels and interaction confirmations.

| Element | Copy | Source |
|---------|------|--------|
| Primary CTA — scan all | "Scan All" | Existing; preserved unchanged |
| Primary CTA — scan item | "Scan Now" | Existing; preserved unchanged |
| Deal badge label | "DEAL" | D-09 — new element on Tier 1 cards |
| Deal badge format | "-{pct}%" (e.g. "-32%") | D-09; shows deal percentage below typical price |
| Stats bar — items label | "items watching" | Existing; preserved |
| Stats bar — deal label | "with deals" | Existing or new; concise muted label below stat value |
| Stats bar — listings label | "listings found" | Existing or new; concise muted label below stat value |
| Stats bar — best price label | "best find" | Existing or new; concise muted label below stat value |
| Disabled scan button tooltip | (none — opacity + cursor communicates state) | D-05; no tooltip needed for personal tool |
| Empty state — no wishlist items | "No records added yet." | Existing; preserved |
| Error state — scan failure | "Scan failed. Try again." | Existing toast pattern; preserved |
| Delete confirmation — dialog | "Delete this record?" | UIP-08 — inline confirmation |
| Delete confirmation — confirm button | "Confirm Delete" | UIP-08 |
| Delete confirmation — cancel button | "Keep Record" | UIP-08; specific label, not generic "Cancel" |
| Typeahead — no results | "Nothing matched — try a different title or artist" | Replaces generic "No results" |
| Typeahead — spinner visible | (no text label — spinner only) | Existing pattern |

**Destructive actions in this phase:**
- Delete wishlist item: inline confirmation already implemented (UIP-08). Confirm button: "Confirm Delete". Cancel button: "Keep Record". No native `confirm()`. Verify regression only.

Source: UIP-08, D-05, D-09 (CONTEXT.md); REQUIREMENTS.md UIP section.

---

## Interaction Contract

### Button States

All three button classes (`.btn-cta`, `.btn-secondary`, `.btn-destructive`) must have all four states. Phase 5 carry-forwards are verified present; only `:disabled` is new.

| State | CSS | Applies To |
|-------|-----|-----------|
| Default | existing | all buttons |
| `:hover` | existing | all buttons |
| `:focus-visible` | `box-shadow: 0 0 0 2px var(--color-accent)` | all buttons (UIP-04 — verified present) |
| `:active` | `transform: scale(0.97)` | all buttons (UIP-05 — verified present) |
| `:disabled` | `opacity: 0.5; cursor: not-allowed; pointer-events: none` | `.btn-cta`, `.btn-secondary` — Scan Now + Scan All during active scan (UIP-06, D-05) |

### Card Interactions

- Card hover: existing shadow + 1px white aura — preserved
- Card is an `<a>` tag — no edit button on card face (locked Phase 3 decision)
- Deal badge on Tier 1 cards: static, no interaction

### Table

- Remove `border-top` from `.table td`
- Row padding: `var(--space-md)` (16px) vertical — standard token, no off-scale overrides
- Right-align numeric columns: price, landed cost
- No hover state change required on table rows (personal tool, low priority)

Source: D-11, D-05 (CONTEXT.md); RESEARCH.md Code Examples.

### Typeahead Spinner

- Spinner must clear on: result selection, type change, empty query
- CSS fix: `.typeahead-spinner.hidden { animation: none; }` stops animation when hidden
- JS path: `spinner.classList.add("hidden")` already called in `selectResult()`, `resetTypeahead()`, type change handler
- If spinner persists after CSS fix: inspect element state in browser devtools before further changes

Source: RESEARCH.md Architecture Patterns / Typeahead Spinner Fix.

### Modal (UIP-07 — verify only)

- `role="dialog"`, `aria-modal="true"`, `aria-labelledby` present
- Focus moves to first input on open; returns to trigger on close
- Verify not regressed — do not reimplement

---

## Component Inventory

No new components are introduced. Existing components are modified.

| Component | File | Change |
|-----------|------|--------|
| Card (`.card`, `.card-body`, `.card-title`) | `static/style.css`, `templates/index.html` | Add tier classes (`.card--deal`, `.card--empty`), apply `--text-body` to card titles |
| Stats bar | `templates/index.html` | Upgrade font sizes: values at `--text-subheading` (22px/700), labels at `--text-label` (12px/400) |
| Table (`.table`) | `static/style.css` | Remove row borders, set row padding to `var(--space-md)`, right-align numerics |
| Button states | `static/style.css` | Add `:disabled` rule |
| H2 headings | `templates/item_detail.html` | Replace inline styles with `--text-subheading` token |
| Typeahead — no-results message | `templates/index.html` | Update copy to "Nothing matched — try a different title or artist" |
| Delete confirmation — cancel button | `templates/index.html`, `templates/item_detail.html` | Label changed from "Cancel" to "Keep Record" |
| Typeahead spinner | `static/style.css` | Add `.typeahead-spinner.hidden { animation: none }` |
| CSS `:root` | `static/style.css` | New tokens: `--text-label` (12px), `--text-subheading` (22px); changed: `--text-heading` (36px); removed: `--text-sm`, `--text-title`, `--text-heading-secondary`, `--text-price` |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | not applicable | not applicable — no shadcn, no third-party component registries |

No external component registries are used. All components are hand-rolled CSS + HTML.

---

## Design Tool Workflow (Required per D-01)

The executor must invoke design tools before and after each implementation wave. This is a locked decision from CONTEXT.md, not a suggestion.

| When | Tool | Purpose |
|------|------|---------|
| Before implementing | `ui-ux-pro-max` — `search.py "vinyl brutalist dark utility personal tool dashboard" --design-system` | Design system reference for brutalist dark patterns |
| Before card tier CSS | `mcp__magic__*` | Card component pattern reference |
| Before writing token changes | `mcp__stitch__*` | Visual screen preview of token changes |
| After each implementation wave | `design-for-ai` CHECKER mode | Confirm hierarchy improvements hold on changed surfaces |

---

## Implementation Fix Order

Per CONTEXT.md D-specifics and design audit convergence:

1. Spacing tiers (`.card-grid` gap → 16px, `.stack-lg` gap → 48px) — highest impact, zero risk
2. Collapse type scale to 4 sizes / 2 weights (add/change/remove tokens in `:root`)
3. Apply type tokens to components (card title → 16px, price → 36px, headings, stats bar)
4. Card tier classification (Jinja2 + CSS `.card--deal`, `.card--empty`)
5. Strip table row borders + set padding to `var(--space-md)` + right-align numerics
6. `:disabled` button state (CSS + JS `startScan()` modification)
7. Typeahead spinner fix (CSS `.typeahead-spinner.hidden`)
8. Replace H2 inline styles with `--text-subheading` token
9. Update "Keep Record" cancel label and "Nothing matched" typeahead copy
10. Verify Phase 5 carry-forwards not regressed (UIP-01, UIP-04, UIP-05, UIP-07, UIP-08, UIP-09, BUG-01)

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

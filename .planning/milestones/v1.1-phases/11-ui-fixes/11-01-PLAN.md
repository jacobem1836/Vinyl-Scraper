---
phase: 11-ui-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/style.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "Page background is warm dark (#0d0b0a), not pure black"
    - "Card surface is #141210, visually distinct from background"
    - "Success colour is muted green #5a9e7a, destructive is muted red #c46060"
    - "Typography scale is reverted: label 12px, body 16px, subheading 20px, heading 24px"
    - "Album artwork remains the visual hero — text is subordinate"
  artifacts:
    - path: "static/style.css"
      provides: "Updated design tokens for warm B&W palette and reverted typography"
      contains: "--color-bg: #0d0b0a"
  key_links:
    - from: "static/style.css"
      to: "all templates"
      via: "CSS custom properties consumed by class selectors"
      pattern: "--color-bg.*#0d0b0a"
---

<objective>
Update all CSS design tokens to implement the warm B&W palette and reverted typography scale.

Purpose: Foundation layer that all subsequent plans depend on. Colour and type tokens flow through custom properties to every component.
Output: Updated `static/style.css` with new token values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ui-fixes/11-CONTEXT.md
@.planning/phases/11-ui-fixes/11-UI-SPEC.md
@static/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update colour tokens for warm B&W palette</name>
  <files>static/style.css</files>
  <action>
In the `:root` block of `static/style.css`, update the following CSS custom properties (per D-03, D-04, D-05):

Colour changes:
- `--color-bg`: `#0a0a0a` -> `#0d0b0a` (warm dark, D-05)
- `--color-surface`: `#111111` -> `#141210` (warm card surface, D-05)
- `--color-surface-alt`: `#0a0a0a` -> `#0d0b0a` (match new bg, D-05)
- `--color-success`: `#34d399` -> `#5a9e7a` (muted green, D-04)
- `--color-destructive`: `#f87171` -> `#c46060` (muted red, D-04)
- `--color-destructive-border`: `rgba(239, 68, 68, 0.4)` -> `rgba(196, 96, 96, 0.4)` (match new destructive)
- `--color-destructive-bg`: `rgba(239, 68, 68, 0.1)` -> `rgba(196, 96, 96, 0.1)` (match new destructive)
- `--color-cta-text`: `#0a0a0a` -> `#0d0b0a` (match new bg for CTA button text)

Also update the type badge tokens to B&W at rest (D-03 — only album artwork has colour):
- `--color-badge-album-bg`: `rgba(100, 120, 180, 0.15)` -> `rgba(255, 255, 255, 0.06)`
- `--color-badge-album-text`: `#a0b0d0` -> `#999999`
- `--color-badge-album-border`: `rgba(100, 120, 180, 0.25)` -> `rgba(255, 255, 255, 0.12)`
- `--color-badge-artist-bg`: `rgba(140, 100, 180, 0.15)` -> `rgba(255, 255, 255, 0.06)`
- `--color-badge-artist-text`: `#b8a0d8` -> `#999999`
- `--color-badge-artist-border`: `rgba(140, 100, 180, 0.25)` -> `rgba(255, 255, 255, 0.12)`
- `--color-badge-other-bg`: `rgba(80, 160, 100, 0.15)` -> `rgba(255, 255, 255, 0.06)`
- `--color-badge-other-text`: `#90c8a0` -> `#999999`
- `--color-badge-other-border`: `rgba(80, 160, 100, 0.25)` -> `rgba(255, 255, 255, 0.12)`

Do NOT change `--color-accent` (#ffffff), `--color-text` (#f5f5f5), `--color-text-muted` (#777777), `--color-text-faint` (#686868), or spacing tokens. Those are correct as-is.
  </action>
  <verify>
    <automated>grep -c "#0d0b0a" static/style.css | xargs test 3 -le</automated>
  </verify>
  <done>All colour tokens updated to warm B&W palette. --color-bg is #0d0b0a, --color-success is #5a9e7a, --color-destructive is #c46060. Type badges are greyscale.</done>
</task>

<task type="auto">
  <name>Task 2: Revert typography scale to pre-Phase-10 values</name>
  <files>static/style.css</files>
  <action>
In the `:root` block, update typography tokens (per D-09, UI-SPEC Typography table):

- `--text-label`: keep at `12px` (unchanged)
- `--text-sm`: keep at `14px` (unchanged, backward compat)
- `--text-body`: keep at `16px` (unchanged)
- `--text-title`: `21px` -> `16px` (card album names drop to body size — artwork is hero, not text)
- `--text-price`: `28px` -> `28px` (unchanged — functional data signal)
- `--text-heading`: `36px` -> `24px` (revert to pre-Phase-10, per D-09)
- `--text-subheading`: `22px` -> `20px` (revert, per UI-SPEC)
- `--text-heading-secondary`: `21px` -> `20px` (match subheading)

Update the `.text-title` helper class: change `font-weight: 500` to `font-weight: 400` (subordinate to artwork).

Update the `.text-heading` helper class line-height: keep at `1.1`.

Update `.text-subheading` helper: `font-weight: 700` -> `font-weight: 600`, `line-height: 1.2` (per UI-SPEC).
  </action>
  <verify>
    <automated>grep -- "--text-heading:" static/style.css | grep "24px"</automated>
  </verify>
  <done>Typography tokens reverted. Heading is 24px, subheading is 20px, title matches body at 16px. Text is subordinate to album artwork.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — CSS-only changes, no user input processing.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | n/a | CSS tokens | accept | Purely visual changes, no security surface |
</threat_model>

<verification>
- Open dashboard in browser, confirm warm dark background (not pure black)
- Cards should appear slightly lighter than background
- All type badges should be greyscale (no blue/purple/green tints)
- Heading text should be noticeably smaller than Phase 10
- Album artwork should be the dominant visual element on cards
</verification>

<success_criteria>
- CSS custom properties updated to exact hex values from UI-SPEC
- No broken selectors or syntax errors in style.css
- Typography scale matches UI-SPEC table (12/16/20/24px)
- B&W palette enforced: no colour on type badges at rest
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-fixes/11-01-SUMMARY.md`
</output>

---
phase: 11-ui-fixes
plan: 05
type: execute
wave: 4
depends_on: ["11-04"]
files_modified:
  - templates/deal_alert.html
autonomous: true
requirements: []
must_haves:
  truths:
    - "Email background is warm dark #0d0b0a"
    - "Email includes CRATE logo or bold text wordmark in header"
    - "Email uses only inline CSS, table layout, system fonts"
    - "Deal highlight colour is muted green #5a9e7a"
    - "Email renders correctly with no CSS custom properties"
  artifacts:
    - path: "templates/deal_alert.html"
      provides: "Updated email template with warm dark palette and logo"
      contains: "#0d0b0a"
  key_links:
    - from: "templates/deal_alert.html"
      to: "static/logo.svg"
      via: "Inline SVG or text fallback in email header"
      pattern: "CRATE"
---

<objective>
Update the deal alert email template to use the warm dark palette, muted deal colour, and CRATE logo.

Purpose: Brand consistency between dashboard and email notifications. Email clients require inline CSS and table layout — no custom properties or modern CSS.
Output: Updated `deal_alert.html` with warm dark palette and logo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-ui-fixes/11-CONTEXT.md
@.planning/phases/11-ui-fixes/11-UI-SPEC.md
@.planning/phases/11-ui-fixes/11-04-SUMMARY.md
@templates/deal_alert.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update email template colours and logo</name>
  <files>templates/deal_alert.html</files>
  <action>
Update `templates/deal_alert.html` with the following changes (per D-11, UI-SPEC Email Contract):

**Colour replacements** (find and replace all inline style hex values):
- `#0a0a0a` -> `#0d0b0a` (warm dark background — appears in body, outer table, header, divider, footer, MSO styles)
- `#111111` -> `#1a1818` (card surface — per UI-SPEC email card surface token)
- `#34d399` -> `#5a9e7a` (deal highlight — the "% below typical" text, line ~57)

**Logo in header** (D-11):
Replace the CRATE wordmark in the header row (line ~31) with an inline SVG of the chosen logo + text wordmark. Since email clients have poor SVG support, use a dual approach:
- For clients that support SVG: include the logo inline
- Fallback: keep the bold "CRATE" text wordmark

The safest approach for maximum email client compatibility:
```html
<td align="center" style="background-color:#0d0b0a; padding:28px 40px 24px 40px; border-bottom:1px solid #222222;">
  <span style="font-family:Arial,Helvetica,sans-serif; font-size:14px; font-weight:700; color:#ffffff; letter-spacing:6px; text-transform:uppercase;">CRATE</span>
</td>
```

Note: Inline SVG in emails is unreliable (Gmail strips it, Outlook ignores it). The safest approach is the bold text wordmark. If the user later wants a hosted image logo, they can add an `<img>` tag pointing to a publicly hosted URL.

**Font update**: Email fonts stay as system stack `Arial, Helvetica, sans-serif` (per UI-SPEC — email clients strip web fonts). Do NOT reference Bodoni Moda or any custom font.

**CTA button copy**: Update "View on CRATE" (line ~113) to "View deal" (per UI-SPEC Copywriting Contract).

**MSO conditional styles**: Update the MSO conditional block (lines ~8-15) to use updated hex values if any `#0a0a0a` references exist there.

**Verify all inline hex values** are updated — do a final pass for any remaining `#0a0a0a` or `#34d399` references.
  </action>
  <verify>
    <automated>python3 -c "content=open('templates/deal_alert.html').read(); assert '#0a0a0a' not in content.split('<!--[if mso]>')[0] or '#0a0a0a' not in content, 'Old bg hex still present'; assert '#34d399' not in content, 'Old success hex still present'; print('PASS')"</automated>
  </verify>
  <done>Email template uses warm dark #0d0b0a background, #1a1818 card surface, #5a9e7a deal highlight. CRATE text wordmark in header. CTA says "View deal". All inline CSS, table layout, system fonts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Email template | Jinja2 variables (item_name, best_landed_price, etc.) rendered into email HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | S (Spoofing) | Email content | accept | Template variables already escaped by Jinja2 autoescaping; no new user input paths added |
</threat_model>

<verification>
- Render the email template with test data (manually or via notifier test)
- Background should be warm dark, not pure black
- Deal percentage text should be muted green, not vivid emerald
- CTA button should say "View deal"
- No CSS custom properties (`var(--...)`) appear in the template
- No flexbox or grid layout — tables only
</verification>

<success_criteria>
- All #0a0a0a replaced with #0d0b0a in email body (MSO block may retain for Outlook compatibility)
- #34d399 replaced with #5a9e7a
- #111111 replaced with #1a1818
- CTA copy is "View deal"
- No CSS custom properties in template
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-fixes/11-05-SUMMARY.md`
</output>

---
phase: 04-ui-polish
plan: 03
type: execute
wave: 1
depends_on: ["04-01", "04-02"]
files_modified: [app/models.py, app/database.py, app/services/scanner.py]
autonomous: true
gap_closure: true
requirements: [POLISH-05, POLISH-06]

must_haves:
  truths:
    - "Scanning completes without IntegrityError when the same store URL appears for two different wishlist items"
    - "After scan, artwork_url is overwritten with the latest high-res cover image from Discogs, even if a thumbnail was already set"
  artifacts:
    - path: "app/models.py"
      provides: "Listing model with composite unique constraint"
      contains: "UniqueConstraint"
    - path: "app/database.py"
      provides: "Migration to drop old url unique index and create composite index"
      contains: "ix_listings_item_url"
    - path: "app/services/scanner.py"
      provides: "Scanner that always updates artwork and inserts per-item listings"
  key_links:
    - from: "app/models.py"
      to: "app/database.py"
      via: "migration drops old index, model declares new composite constraint"
      pattern: "UniqueConstraint.*wishlist_item_id.*url"
    - from: "app/services/scanner.py"
      to: "app/models.py"
      via: "INSERT succeeds because constraint is now per-item"
      pattern: "db\\.add\\(listing\\)"
---

<objective>
Close two UAT gaps from test 7: (1) scan crash caused by global UNIQUE(url) on listings table, and (2) artwork not updating because of an early-exit guard.

Purpose: Unblock scanning so it completes without IntegrityError, and ensure every scan overwrites artwork_url with the latest high-res image.
Output: Patched models.py, database.py migration, and scanner.py logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-polish/04-UAT.md

@app/models.py
@app/database.py
@app/services/scanner.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix UNIQUE constraint from global to per-item composite</name>
  <files>app/models.py, app/database.py</files>
  <action>
In app/models.py, on the Listing class:
1. Remove `unique=True` from the `url` Column definition (line 35: change `url = Column(String, nullable=False, unique=True)` to `url = Column(String, nullable=False)`)
2. Add a `__table_args__` tuple to the Listing class with a UniqueConstraint:
   ```python
   __table_args__ = (
       UniqueConstraint("wishlist_item_id", "url", name="uq_listing_item_url"),
   )
   ```
3. Add `UniqueConstraint` to the sqlalchemy import at line 1.

In app/database.py, add a new migration block inside `run_migrations()` that:
1. Drops the old unique index on `listings.url`. For SQLite this is `DROP INDEX IF EXISTS ix_listings_url`. For PostgreSQL: `DROP INDEX IF EXISTS ix_listings_url`.
   Also try dropping the auto-generated constraint name: `DROP INDEX IF EXISTS "uq_listings_url"` (wrap in try/except pass).
2. Creates the new composite unique index: `CREATE UNIQUE INDEX IF NOT EXISTS uq_listing_item_url ON listings (wishlist_item_id, url)`.
   Wrap each statement in its own try/except pass block, matching the existing migration pattern in database.py.
  </action>
  <verify>
    <automated>cd /Users/jacobmarriott/Documents/Personal/Vinyl-Scraper && python -c "
from app.database import Base, engine, run_migrations
from app.models import Listing
import sqlalchemy
run_migrations()
Base.metadata.create_all(bind=engine)
insp = sqlalchemy.inspect(engine)
indexes = insp.get_indexes('listings')
unique_names = [idx['name'] for idx in indexes if idx.get('unique')]
cols = [col for idx in indexes if idx.get('name') == 'uq_listing_item_url' for col in idx['column_names']]
assert 'uq_listing_item_url' in unique_names, f'Missing composite index. Found: {unique_names}'
assert cols == ['wishlist_item_id', 'url'], f'Wrong columns: {cols}'
print('PASS: composite unique constraint exists on (wishlist_item_id, url)')
"
    </automated>
  </verify>
  <done>Listing model has UniqueConstraint on (wishlist_item_id, url), old global unique on url is dropped, migration runs without error</done>
</task>

<task type="auto">
  <name>Task 2: Always overwrite artwork_url on scan</name>
  <files>app/services/scanner.py</files>
  <action>
In app/services/scanner.py, modify the artwork extraction block (lines 28-37 and line 80):

1. Remove the `if not item.artwork_url:` guard at line 30. The artwork extraction should ALWAYS run, not just when artwork_url is None. Replace lines 28-37 with:
   ```python
   # Extract artwork URL from scan results (always overwrite with latest high-res image)
   cover_image = None
   for r in all_results:
       ci = r.pop("_cover_image", None)
       if ci and not cover_image:
           cover_image = ci
       # Still pop from remaining results even after we have one
   for r in all_results:
       r.pop("_cover_image", None)
   ```
   (The second loop catches any remaining keys not yet popped.)

2. At line 80, remove the `and not item.artwork_url` condition. Change:
   `if cover_image and not item.artwork_url:` to `if cover_image:`
   so artwork_url is always updated with the latest cover image from Discogs.
  </action>
  <verify>
    <automated>cd /Users/jacobmarriott/Documents/Personal/Vinyl-Scraper && python -c "
import ast, sys
with open('app/services/scanner.py') as f:
    source = f.read()
# Verify the guard is removed
assert 'if not item.artwork_url' not in source, 'Old guard still present'
assert 'if cover_image and not item.artwork_url' not in source, 'Old conditional still present'
assert 'if cover_image:' in source, 'New unconditional assignment missing'
print('PASS: artwork_url guard removed, always-overwrite logic in place')
"
    </automated>
  </verify>
  <done>Scanner always extracts _cover_image from results and overwrites item.artwork_url on every scan, regardless of whether artwork was previously set</done>
</task>

</tasks>

<verification>
1. Both tasks' automated verify commands pass
2. Manual smoke test: run the app locally, trigger a scan for an item that already has a low-res thumbnail — it should complete without IntegrityError and the artwork_url should update to a high-res URL
</verification>

<success_criteria>
- Scanning multiple wishlist items whose listings share URLs (e.g. same store) completes without IntegrityError
- Existing items with old thumbnail artwork_url get overwritten with high-res cover on next scan
- No regressions: existing listings are still deduplicated per (wishlist_item_id, url)
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-polish/04-03-SUMMARY.md`
</output>

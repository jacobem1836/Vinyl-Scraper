---
phase: quick
plan: 260414-d0c
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/base.html
autonomous: true
requirements: []

must_haves:
  truths:
    - "While scan is running, a toast shows 'Scanning (N/total) — item_name' and updates each poll cycle"
    - "When scan completes, the completion toast 'N new listings found' replaces the scan-progress toast"
    - "No backend changes required"
  artifacts:
    - path: "templates/base.html"
      provides: "Updated renderStatus() with persistent in-progress toast"
  key_links:
    - from: "renderStatus() is_running branch"
      to: "window.showToast()"
      via: "called each poll cycle with large durationMs to prevent auto-dismiss"
---

<objective>
Update `renderStatus()` in `templates/base.html` to show a persistent, updating toast while a scan is running.

Purpose: Users get immediate ambient feedback about scan progress without needing to open the scan panel.
Output: Modified `renderStatus()` that calls `showToast()` on every poll cycle while running, then replaces it with the completion message when done.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

How `showToast` works (extracted from templates/base.html lines 108-119):
- Single `#toast` DOM element — no IDs, no registry
- Calling `showToast(message, durationMs)` replaces the current message and resets the hide timer
- Passing a very large `durationMs` (e.g. `999999`) keeps the toast visible indefinitely
- Calling `showToast(newMsg, normalMs)` from the completion branch naturally replaces the in-progress toast
- No new mechanism needed — re-calling showToast each poll cycle is sufficient
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add in-progress toast to renderStatus()</name>
  <files>templates/base.html</files>
  <action>
In `renderStatus()`, inside the `if (s.is_running)` branch (around line 235), add a call to `window.showToast()` after the existing pill/active-element updates:

```js
var currentName = (s.current && s.current.length > 0) ? s.current[0].query : 'items';
window.showToast('Scanning (' + s.done + '/' + s.total + ') \u2014 ' + currentName, 999999);
```

The `999999` ms duration keeps the toast visible until the next call replaces it. The existing completion branch (the `else` block, around line 241) already calls `window.showToast(...)` — that call stays exactly as-is and will naturally dismiss the in-progress toast by resetting the timer to 6000ms.

No other changes needed. Do not modify the completion branch, the scan panel, or any other logic.
  </action>
  <verify>
Open the app locally, trigger a scan, and observe:
1. A toast appears immediately showing "Scanning (0/N) — item_name"
2. The toast text updates each ~2s poll cycle with new progress
3. When scan finishes, the toast changes to "N new listings found" and auto-dismisses after ~6s
  </verify>
  <done>
- In-progress toast appears and updates while scan runs
- Completion toast replaces it on finish
- No regressions to scan panel, log, or pill text
  </done>
</task>

</tasks>

<verification>
Manual: start a scan and confirm the toast updates every ~2 seconds with current item name and progress counter.
</verification>

<success_criteria>
- Toast visible throughout scan duration showing live N/total and current item name
- Toast replaced by completion message when scan ends
- No JS errors in console
</success_criteria>

<output>
No SUMMARY.md required for quick tasks.
</output>
